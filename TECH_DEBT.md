# TECH_DEBT — «Оборот»

> Ранжированный технический долг на `0fd3783`. Ранг — **моя оценка**, исходя из
> того, что это аналитический продукт для B2B: неверная цифра дороже упавшей
> страницы, а потеря чужих данных дороже всего. Ничего не проверялось на живом
> стенде; уровень уверенности указан у каждого пункта.
>
> Обозначения уверенности: **[подтверждено]** — прочитан код целиком по этому пути;
> **[вероятно]** — логика прослежена, но сценарий не воспроизведён;
> **[гипотеза]** — вывод по косвенным признакам, требует проверки.

---

## A. Корректность и сохранность данных

Для продукта, который говорит владельцу бренда «закажи вот столько на 400 тысяч»,
это единственная категория, где ошибка стоит денег клиента.

### DATA-1. Повтор `POST` при таймауте создаёт дубликат заказа поставщику в МойСкладе
**[закрыто 24.08.2026, кроме живой проверки контракта]** · влияние было: высокое

Было: отправка шла через общий механизм ретраев, ключа идемпотентности не было,
а «свой» документ искался по метке `[oborot#<id>]` в описании. Метку правит
человек, а `<id>` — переиспользуемый rowid SQLite, поэтому новый заказ на
освободившемся rowid усыновлял документ удалённого.

Сделано (D-36, D-37): стабильный uuid4 `ms_sync_id` рождается и коммитится ДО
сети и уходит в МойСклад полем `syncId` (повторный POST с занятым ключом
обновляет документ, а не создаёт второй); ветки «POST без syncId» не
существует — запрет в `app/ms_client.py`; поиск по метке оставлен только
строкам с явным `ms_lookup_mode='legacy'`; контрагент «Производство» получает
свой `syncId` и закреплённую ссылку, при нескольких одноимённых — 409 с
перечислением вместо выбора наугад. Красный/зелёный —
`tests/test_writeback_idempotency.py` (70 проверок).

**Что ещё открыто — и это условие слияния.** Поведение `syncId` закреплено
МОКОМ. Мок — модель чужого API, а не доказательство: официальный контракт
JSON API 1.2 обещает уникальность `syncId` и upsert по нему, но на боевом
аккаунте это никем из нас не проверено. Нужен live contract-test на реальном
токене — `tests/test_ms_syncid_live.py`.

**Гейт считается пройденным, только если выполнено ВСЁ перечисленное — и
отдельно для контрагента, и отдельно для заказа поставщику:**

1. живой `POST` **принимает** наш `syncId` (а не отвечает 412);
2. **повторный** `POST` с ТЕМ ЖЕ `syncId` возвращает **ТОТ ЖЕ** `id` — то
   есть обновляет уже созданную сущность, а не заводит вторую;
3. **после первого** `POST` эквивалентный продуктовому **ограниченный перебор
   коллекции** (`MoySkladClient.find_by_sync_id`: постраничный обход с ТОЧНЫМ
   сравнением `syncId` и явной границей) находит **ровно одну** сущность с
   этим ключом — и это именно созданная нами;
4. **после повторного** `POST` тот же перебор снова находит **ровно одну**
   сущность с этим ключом.

Пункты 3 и 4 названы отдельно намеренно: «второго документа не появилось» —
это утверждение о состоянии аккаунта ПОСЛЕ повтора, и доказывать его обязан
пересчёт, а не память о первом ответе.

**Почему перебор, а не фильтр.** 25.08.2026 живой read-only preflight получил
на `GET /entity/counterparty?filter=syncId=<uuid>` ответ **HTTP 412, code
1034, «неизвестное поле фильтрации syncId»** (журнал — Issue координации,
[комментарий 5414290329](https://github.com/chernim-cherno-analytics/oborot-saas/issues/2#issuecomment-5414290329)).
Документация МойСклада при этом обещает обратное — в таблицах полей
контрагента и заказа поставщику у `syncId` стоят операторы `=` и `!=`. Между
обещанием документа и наблюдаемым ответом выбран ответ, и продукт с тех пор
на этот фильтр не опирается вовсе.

Поэтому **поведение `filter=syncId` — зафиксированное НАБЛЮДЕНИЕ, а не
требование гейта.** Живой тест по-прежнему пробует фильтр в разведке и
записывает исход (вдруг МойСклад однажды его включит), но **любой** результат
этой пробы — и 412, и успех — гейт не блокирует и не засчитывает. Прежняя
редакция этого абзаца требовала, чтобы фильтр находил документ; такое условие
слияния невыполнимо по факту и отправляло проверяющего тестировать путь,
которого в продукте нет.

Если живой API отвергнет `syncId` при создании или поведёт себя иначе в любом
из четырёх пунктов — правку **не сливать**, а пересматривать дизайн; условной
развилки «а если не поддержит» в опасном пути не будет сознательно (см. D-36,
пункт 2).

### DATA-2. Падение `commit` после успешной отправки теряет ссылку и оставляет вечный лок
**[закрыто 24.08.2026]** · влияние было: высокое

Было: коммит после отправки не обёрнут в обработку ошибок, а появившийся позже
фолбэк «сохраним хотя бы ссылку» был не лучше — он превращал сбой в вечный
двойной счёт «едет к нам» без следа в логах.

Сделано (D-37): `T2` — ссылка на документ и перенос вклада между `qty` и
`ms_qty` — выполняется одной транзакцией; при сбое повторяется целиком; при
повторном сбое отдаётся честный третий исход `WritebackUnknown` («документ
создан, сохранить у нас не удалось»), а не половина правды. Ключ
идемпотентности остаётся в строке, поэтому повтор безопасен, и ближайший синк
связывает документ с заказом по `syncId` сам
(`app/ms_sync._backmatch_by_sync_id`) — без отдельного ночного job и без
ослабления правила принадлежности D-28.

### ~~DATA-3. Остатки и продажи за один период публиковались порознь~~
**ЗАКРЫТО 23.08.2026** · влияние было: **критическое**

Формулировка долга («падение между стиранием остатков и перезаписью продаж
оставляет организацию с одним днём остатков и полным годом продаж») описывала
шов, закрытый ещё 18.08: `_write_stock_rows(..., wipe=True, wipe_sales=True)`
стирает обе таблицы одной транзакцией, покрытие — `tests/test_sync_wipe.py`.

Открытым оставался **шов ниже** — между записью остатков периода и загрузкой
продаж ЗА ТОТ ЖЕ период. Окно быстрого старта и каждый кусок истории
публиковались в два приёма: коммит остатков → десятки секунд сети → коммит
продаж. Граница загруженной истории берётся аналитикой из таблицы остатков
(`coverage_start = min(StockDay.date)`), поэтому всё это время «дней в стоке»
было больше, чем дней с продажами: темп занижен, «хватит на N дней» завышено,
клиент недозаказывает. Штатно это видел любой, кто смотрел на страницу во
время пересборки уже активной организации; падение в окне (429, OOM, рестарт,
деплой) оставляло такую базу до следующего успешного прогона. Тот же разрыв
на один день был в почасовом инкременте.

Тот же разрыв, но найденный позже (независимое ревью 25.08.2026), был и в
ПРОДОЛЖЕНИИ прерванной первичной загрузки: остатки пропущенных дней
публиковались, граница загруженной истории уезжала вперёд, и только потом
отдельным вызовом ехали продажи. Сдвинутая граница здесь хуже самой дыры —
следующий запуск переставал считать эти дни пропущенными. А при разрыве
ровно в один день с уже закрытым окном продажи этого дня не грузились этим
путём вообще, и никакого сбоя при этом не происходило.

Исправление (D-38): обе половины периода скачиваются целиком и коммитятся
одной транзакцией — окно, каждый кусок истории, инкремент и дни разрыва при
продолжении. Смерть до коммита оставляет период не загруженным, и продолжение
чинит это само.
Покрытие — `tests/test_sync_atomic.py` (падение на продажах старого куска,
падение на продажах окна, продолжение, инкремент, многодневный разрыв при
продолжении, однодневный разрыв при закрытом окне — на отказе и на успехе).

### DATA-4. История никогда не перечитывается, сверки с источником нет
**[подтверждено]** · влияние: высокое, накопительное · стоимость: 2–3 дня

После первичной загрузки инкремент трогает только окно фактического простоя.
Документ, проведённый задним числом за пределами окна, изменение цены в старом
документе, распроведение или удаление — не попадут в систему **никогда**.
Остатки за прошлые даты не пересчитываются вообще. Расхождение с МойСкладом
может только накапливаться, и механизма его обнаружения не существует.

**Здесь у проекта есть редкое преимущество:** работающая первая таблица на тех же
данных с открытыми API. Периодическая сверка двух реализаций закрывает и этот
пункт, и следующий.

### DATA-5. Нерасследованное расхождение с эталоном за май
**[гипотеза, факт расхождения — подтверждён журналом]** · влияние: высокое

11,8 млн ₽ против 18,95 млн ₽ у первой таблицы за один и тот же месяц.
Причина не установлена. Пока она не найдена, **доверие ко всем историческим
цифрам ограничено**, и это важнее любого рефакторинга.

### DATA-6. Гонка отправки заказа с импортом «Заказов поставщику» обнуляет «едет к нам»
**[вероятно]** · влияние: среднее · стоимость: часы

Импорт сначала читает документы из МойСклада, потом обнуляет колонку и пишет
заново. Заказ, отправленный между чтением и записью, исчезнет из «едет к нам»
до следующего синка — а рекомендации к закупке на это время завысятся.

### DATA-7. Частичное сопоставление позиций систематически занижает «едет к нам»
**[закрыто]** · влияние было: среднее · стоимость: часы

При отправке из локального счётчика вычиталось **полное** количество позиции,
а во внешний прибавлялось только сопоставленное. Несопоставленные позиции
исчезали из «едет» навсегда: в МойСкладе документа на них нет, а локальный вклад
снимался целиком. Пользователь видел список несопоставленных ровно один раз,
в ответе на отправку — восстановить пропажу было нечем.

**Исправлено:** `_move_incoming_to_ms` (`app/ms_writeback.py`) снимает из
локального `qty` ровно ту величину, что реально попала в документ МойСклад
(`pushed_by_base`, агрегирована по base_name+size), а не полное количество
позиции заказа. Несопоставленный остаток — будь то целиком несопоставленный
`base_name` или часть размеров внутри одного — в `qty` и остаётся. Закрыто
RED/GREEN на два случая (частичный матч между двумя base_name и частичный матч
размеров внутри одного base_name) в `tests/test_writeback.py`.

### ~~DATA-8. Тихие потери при синхронизации~~
**закрыто 27.08.2026** (было: подтверждено · влияние среднее · стоимость 1 день)

Три независимых сценария, объединённых свойством «наружу уходит только счётчик,
а часто и его нет»:
* ~~товары, созданные в МойСкладе между чтением ассортимента и отчётом
  остатков, теряются на весь прогон.~~ **Закрыто.** `_run_initial` и
  `_run_incremental` строят `ext_to_pid` один раз, сразу после чтения
  ассортимента (`_upsert_products`), а отчёт остатков того же прогона читает
  этот же справочник позже — товар, созданный в МойСкладе между этими двумя
  чтениями, не находился и молча уходил в `unmatched`. Теперь после
  каждого чтения остатков, если остался непустой `unmatched`,
  `_reconcile_late_products` (`app/ms_sync.py`) делает ОДИН ограниченный
  дополнительный проход: перечитывает ассортимент ещё раз, заводит товары
  для той части `unmatched`, что нашлась, расширяет `ext_to_pid` и просит
  вызывающий код перечитать остатки за те же даты ещё раз. Если повторное
  чтение ассортимента само упало (сеть/лимит) или ext_id так и не
  нашёлся — реальной гонки не случилось, это нераспознанный SKU — код
  молчит и оставляет остаток исключённым, как и раньше: без фабрикации
  данных и без второй попытки (fail-closed). Регрессия —
  `tests/test_sync_late_product.py`: RED на прежнем коде (только что
  созданный товар терялся, `late_products_recovered` не писался); GREEN
  заводит товар и его остаток, а второй сценарий на той же организации
  проверяет, что товар-призрак (никогда не находится) по-прежнему честно
  считается в `stock_unmatched_skus` и не выдумывается.
* ~~документы продаж без распознанного склада отбрасываются целиком
  (счётчик есть, в интерфейс не выводится, порога тревоги нет).~~ **Закрыто
  27.08.2026.** Отбор продаж и сам счётчик `stats["sales_docs_skipped_store"]`
  (`_collect_sales`, `app/ms_sync.py`) не менялись — правка узкая, чисто
  диагностическая. `get_status()` теперь отдаёт `diagnostics.sales_docs_skipped_store`
  owner-only (`/api/sync/status`): точный неотрицательный `int` из `stats`
  проходит как есть, а отсутствие поля, `bool`, отрицательное число, строка
  или float — fail-closed `None` (`_bounded_diagnostic_count`), никогда не
  подменяется нулём. Публичный `/api/sync/progress` (участник) диагностику и
  `stats` по-прежнему не отдаёт — контракт не расширялся. На «Настройках»
  положительный счётчик показывается владельцу понятной фразой про
  нераспознанный/невыбранный склад документа; ноль или отсутствие — без
  единого тревожного сообщения. Диагностика не блокирует синк и не участвует
  в расчётах. Регрессия — `tests/test_sync_diag_store.py` (19 проверок:
  RED на прежнем коде 13 OK/6 FAIL — ровно на контракте diagnostics, GREEN
  доказывает точный положительный счётчик, честный ноль, fail-closed на семи
  видах некорректных значений, схему из одного именованного поля, изоляцию
  между организациями и границу владелец/участник) и сценарий в
  `tests/test_ui.py` (владелец видит положительный счётчик понятным текстом
  на «Настройках», ноль — без сообщения; RED на прежней вёрстке 30 OK/3 FAIL
  ровно на этих трёх проверках, GREEN 33/0).

  **Corrective (независимое ревью BLOCKED, issuecomment-5438193835,
  27.08.2026): первая версия читала diagnostics ПРЯМО из ephemeral
  `stats.sales_docs_skipped_store`, который `_collect_sales` обнуляет на
  КАЖДОМ прогоне (`if replace_all or not initial: ... = 0`).** Обычный
  успешный инкремент перечитывает только свой узкий хвост (DATA-4: старые
  документы никогда не перечитываются заново) — его честный ноль тихо гасил
  предупреждение о пропусках, найденных более ранним прогоном, хотя
  проблемные документы никуда не делись. Хуже того: `start_sync`/`_run_sync`
  переносят между прогонами только ключи `_CARRIED_STATS`, поэтому
  diagnostics обнулялась уже в момент СТАРТА следующего синка (до первого
  `_collect_sales`), а не только на его завершении.

  Теперь факт хранится отдельно от ephemeral-счётчика — `stats.
  sales_docs_skipped_store_unresolved` (тоже в `_CARRIED_STATS`, переживает
  каждый новый запуск) — и обновляется РОВНО один раз, из
  `_apply_skip_diagnostic_lifecycle` в финализации `_run_sync`, куда упавший
  прогон не доходит (структурная гарантия «failed/partial не трогает
  факт», а не отдельная проверка состояния). Точная таблица (уточнена
  вторым corrective ниже — первая формулировка здесь ошибочно писала, что
  resumed «не трогает факт ни при положительном, ни при нулевом» итоге, что
  противоречило коду и было исправлено): любой прогон (обычный инкремент,
  RESUMED первичная загрузка, полная пересборка) со своим ПОЛОЖИТЕЛЬНЫМ
  итогом обновляет факт на это свежее значение — свежая находка не должна
  прятаться за старым числом, независимо от охвата истории; собственный
  ЧЕСТНЫЙ НОЛЬ обычного инкремента или RESUMED первичной загрузки (её
  счётчик покрывает только чанки, докачанные в ЭТОМ прогоне, а не всю
  историю — `resumed_from` выставлен в `_run_initial`) факт НЕ очищает;
  авторитетно очистить факт в 0 может только НЕрезюмированная первичная
  загрузка/полная пересборка СО СВОИМ нулём — она одна проходит по всей
  истории заново. UI-текст переписан: «синхронизация пропускала N
  документов, данные могут быть неполными, полная пересборка (после
  проверки складов) перепроверит их» — вместо утверждения точного текущего
  итога.

  Регрессия (часть 2 `tests/test_sync_diag_store.py`, реальный синк через
  мок МойСклад, а не прямая запись `stats_json`): RED на версии до corrective
  — 24 OK/11 FAIL, включая дословную репродукцию обеих находок независимого
  ревью (`diagnostics=None` сразу после старта инкремента при
  `state=running`; `diagnostics=0` после честного нуля обычного инкремента,
  хотя старые пропуски не перечитывались). GREEN после corrective — 35/0:
  первичная загрузка с пропусками → факт положительный авторитетно; старт
  инкремента сохраняет факт; завершённый инкремент с честным нулём факт НЕ
  гасит; упавшая на 429 пересборка факт не трогает; успешная
  нерезюмированная пересборка с найденным нулём (документы устранены) факт
  гасит. `tests/test_ui.py` подтверждает то же на уровне рендера (34/0).

  **Второй corrective (независимое ревью BLOCKED, issuecomment-5438529547,
  27.08.2026): rollout из production/main терял уже существующий
  положительный факт.** До появления sticky-ключа прод уже писал
  ephemeral `stats.sales_docs_skipped_store` на каждом прогоне; первая
  версия читала diagnostics только из sticky и переносила между прогонами
  только `_CARRIED_STATS` с новым ключом — legacy-значение отбрасывалось.
  Итог: у организации с уже сохранённым положительным legacy-счётчиком
  факт был НЕВИДИМ владельцу сразу после выпуска правки, а первый же
  `start_sync` стирал его окончательно (carried-словарь заменяет собой всю
  строку, а legacy-ключ никогда не входил и не должен входить в
  `_CARRIED_STATS`) — тот же silent-loss на границе релиза, который правка
  и должна была закрыть. Отдельно: тест «partial rebuild preserves»
  первого захода падал на ассортименте ДО того, как первичная загрузка
  успевала сохранить resume point, и не доказывал ветку кода, которая
  реально принимает решение по `stats["resumed_from"]`.

  Теперь `_resolve_skip_diagnostic` — единая точка чтения: пока sticky-ключ
  ХОТЬ РАЗ не записан, диагностика берётся из legacy, но только если
  legacy — точный ПОЛОЖИТЕЛЬНЫЙ `int` (ноль/отсутствие/`bool`/отрицательное/
  строка/float ничего не бутстрапят, как и раньше — fail-closed). Если
  sticky-ключ уже присутствует (даже malformed-значением) — он приоритетен
  и НЕ подменяется legacy: новый код уже принял решение по этому прогону.
  `_carry_stats` — общая точка, которой пользуются и `start_sync`, и
  `_run_sync`: она делает тот же bootstrap ПЕРЕД тем, как carried-словарь
  заменит собой всю строку, поэтому факт переживает не только чтение статуса,
  но и сам переход в queued/running состояние следующего прогона.

  Регрессия (`tests/test_sync_diag_store.py`): RED на версии первого
  corrective (`6e1d544`) — 68 OK/4 FAIL, ровно на новых сценариях rollout
  (bootstrap до первого прогона; carry через queued/running и честный ноль
  инкремента). GREEN после второго corrective — 72/0: плюс настоящая частичная
  первичная загрузка, реально сохранившая resume point (`history_loaded_from`
  записан фазой month/finalize-lite) и упавшая ВНУТРИ фазы history — а не на
  ассортименте, — с последующим успешным резюме, чей собственный честный ноль
  факт не очищает; плюс прямая юнит-проверка `_apply_skip_diagnostic_lifecycle`
  на всей таблице решений, включая resumed-положительный → обновление.

  **Третий corrective (независимое ревью, P2 discussion_r3871610382,
  27.08.2026): уточнение второго захода — «sticky уже присутствует — он
  приоритетен и НЕ подменяется legacy» было верно ТОЛЬКО для чтения
  (`get_status`/`_resolve_skip_diagnostic`), но `_carry_stats` дословно
  повторял то же условие и для carry, а это другая ситуация.** Прерванный
  (упавший) прогон мог найти НОВОЕ положительное raw (например 8) уже
  ПОСЛЕ того, как sticky был 5 из более раннего прогона: `_collect_sales`
  успевает накопить счётчик в фазе month, прогон падает позже, в фазе
  history. Второй corrective в этом случае оставлял carried sticky равным
  старому 5, потому что sticky-ключ уже существовал — свежая находка
  упавшего прогона терялась НАВСЕГДА, потому что resume не перечитывает
  уже обработанные чанки (тот же принцип DATA-4, что и у обычного
  инкремента). Теперь `_carry_stats` проверяет свежий legacy БЕЗУСЛОВНО:
  валидный положительный `sales_docs_skipped_store` из предыдущей строки
  всегда материализуется в sticky, даже когда старый sticky уже был —
  это покрывает и исходный rollout-случай (sticky отсутствует), и этот,
  более узкий (sticky есть, но устарел). `_resolve_skip_diagnostic`
  (чтение) не менялся — его правило «sticky приоритетен» остаётся верным
  ровно там, где и было: между carry-обновлениями строку никто не читает
  иначе, чем через уже актуальный sticky.

  Регрессия: RED на версии второго corrective (`99692ca`) — 89 OK/3 FAIL,
  ровно на новом сценарии (error snapshot честно хранит raw=8 рядом со
  старым sticky=5; следующий resume обязан показать 8 сразу на
  queued/running снимке и сохранить 8 после собственного честного нуля) и
  на прямой юнит-проверке `_carry_stats`. GREEN после третьего corrective —
  92/0.

  **Четвёртый corrective (независимое ревью, P2 discussion_r3871933011,
  27.08.2026): третий заход считал только ОДИН сегмент прерванной
  пересборки — «свежий raw побеждает старый sticky» справедливо, если
  находка ровно одна.** Если полная пересборка распадается на НЕСКОЛЬКО
  прогонов (изначальный + один или несколько resume), а находки в РАЗНЫХ
  disjoint-сегментах (чанки, обработанные ДО очередного падения, никогда
  не перечитываются заново — тот же принцип DATA-4) оба положительны —
  третий corrective подменял sticky ПОСЛЕДНИМ сегментом вместо суммы: 8
  (завершённые до первого падения чанки) + 3 (докачанный резюмированный
  остаток) давали sticky=3, а не 11. Теряя более ранние disjoint-находки,
  это воспроизводило тот же класс дефекта, который третий corrective уже
  закрыл для СТАРОГО sticky, но не заметил для СВОЕЙ ЖЕ бухгалтерии на
  границе успешного завершения.

  Теперь `_apply_skip_diagnostic_lifecycle` и `_carry_stats` считают не
  голый `raw`, а `_rebuild_total` = raw ТЕКУЩЕГО сегмента + новый ключ
  `sales_docs_skipped_store_rebuild_checkpoint` — сумма ВСЕХ предыдущих
  сегментов ТОЙ ЖЕ логической пересборки. Checkpoint переносится и
  СКЛАДЫВАЕТСЯ (не заменяется) в `_carry_stats` при `resuming=True`,
  переживая сколько угодно повторных падений/резюме подряд; он в
  `_CARRIED_STATS` (как и sticky) — иначе терялся бы уже на повторном
  внутреннем вызове `_carry_stats` (`start_sync` пишет queued-строку,
  `_run_sync` тут же перечитывает её заново, а на этом втором вызове
  ephemeral `sales_docs_skipped_store` уже отсутствует, `_rebuild_total`
  честно возвращает `None`, и без базового копирования checkpoint терялся
  бы уже на СТАРТЕ, а не только между прогонами). Checkpoint снимается при
  успешном завершении (пересборка закончилась) и при старте НОВОЙ, не
  резюмированной пересборки (`resuming=False` — своя старая бухгалтерия не
  наследуется). Для обычного инкремента и нерезюмированной пересборки
  checkpoint всегда отсутствует/0, поэтому `_rebuild_total` там равен
  голому raw — поведение для этих путей не меняется по факту, только по
  источнику числа.

  Регрессия: RED на версии третьего corrective (`ff9110a`) — 97 OK/6 FAIL
  до детерминированного `TypeError` на прямом юнит-вызове с новым
  keyword-параметром `resuming` (сам параметр ещё не существовал —
  честное доказательство отсутствия механизма), причём все 6 FAIL уже до
  падения были ровно на новых чекпоинт-проверках (интеграционные и прямые
  юнит-тесты `_apply_skip_diagnostic_lifecycle`), а не где-то ещё. GREEN
  после четвёртого corrective — 125/0: три последовательных раунда одной
  логической пересборки (8 → +2 при повторном падении → +0 при успешном
  завершении) дают итог 10, а не 2 (последний сегмент) и не 8 (первый,
  забытый); прямая юнит-проверка доказывает и цепочку из трёх подряд
  падений, и то, что checkpoint переживает повторный внутренний вызов
  `_carry_stats` внутри одного и того же старта прогона.

  **Пятый corrective (независимое ревью, P2 discussion_r3872223834,
  27.08.2026): четвёртый заход считал ВЕСЬ raw ТЕКУЩЕГО сегмента безопасным,
  хотя resume перечитывает не только новые, но иногда и УЖЕ посчитанные
  документы — тот же диапазон дат может задвоиться.** Два конкретных случая:
  (A) резюме ТОГО ЖЕ дня форсированно перечитывает «сегодня»
  (`gap_dates=[today_iso]` в `_run_initial`, даже когда реального разрыва
  нет — см. код там) — документ, уже посчитанный месячным окном ДО падения,
  считался ещё раз: checkpoint(8) + raw(8, те же документы) = 16 вместо 8;
  (B) если прогон падает ПОСЛЕ того, как `_collect_sales` увеличил счётчик
  для чанка, но ДО того, как для этого чанка продвинулась
  `history_loaded_from` (например, `_full_positions` упал на документе
  ПОСЛЕ уже посчитанного в том же чанке пропуска), следующий resume
  перечитывает ТОТ ЖЕ чанк заново — ровно численный пример независимого
  ревью: checkpoint(8) + partial(3, недокоммиченный) + retried(3, тот же
  чанк повторно) = 14 вместо 11.

  Первая попытка починить это границами (`history_loaded_from`/
  `history_loaded_to` продвинулись — значит безопасно) провалила
  СОБСТВЕННЫЙ regression-прогон: существующий тест `run_part2_rebuild_
  accumulator` (четвёртый corrective) кладёт на «сегодня» ДВА НОВЫХ,
  ранее не существовавших документа МЕЖДУ раундами — с точки зрения
  диапазона дат это НЕРАЗЛИЧИМО от случая (A) (тот же день, форсированная
  перечитка), но по существу это ДРУГОЙ случай: генуинно новые документы,
  появившиеся между прогонами, обязаны засчитаться. Границы дат этого не
  различают в принципе — синк умеет читать документы по датам, но не умеет
  спрашивать МойСклад «что изменилось с прошлого раза».

  Решение — дедуп ПО ID ДОКУМЕНТА, а не по диапазону дат. `_collect_sales`
  пишет id каждого пропущенного документа в `sales_docs_skipped_store_ids`
  рядом с существующим счётчиком (сам счётчик, отбор продаж и sync control
  flow не изменились). Новая `_commit_skip_ids` (`app/ms_sync.py`),
  вызываемая из `_run_initial` в тех же точках, где четвёртый и первая
  попытка пятого corrective уже отмечали «безопасно» (после фазы today,
  после фазы month, после каждого закоммиченного чанка истории), сверяет
  найденные id с уже подтверждённым набором (`sales_docs_skipped_store_
  committed_ids`, переносится между прогонами через `_CARRIED_STATS` по
  той же причине, что и checkpoint) и добавляет в `sales_docs_skipped_
  store_committed` только НОВЫЕ id. Случай (A): те же id уже в
  подтверждённом наборе — новых 0. Случай четвёртого corrective (новые
  документы на той же дате): id никогда не встречались — засчитываются
  полностью. Случай (B): упавший чанк не доходит до своего вызова
  `_commit_skip_ids` — его id не подтверждаются; резюме перечитывает тот
  же чанк, находит ТЕ ЖЕ id заново и на этот раз, дойдя до коммита,
  подтверждает их ровно один раз.

  Регрессия: RED на версии четвёртого corrective (`40ccaad`) — 147 OK/2
  FAIL, затем детерминированный `AttributeError` на прямом юнит-вызове
  `_skip_segment_contribution` (функция ещё не существовала). Оба FAIL —
  ровно дословное воспроизведение находок ревью: сценарий A даёт
  `diagnostics=16` вместо 8, сценарий B даёт `diagnostics=14` вместо 11
  (точное число из комментария ревью). Промежуточная попытка на границах
  дат прошла оба новых сценария, но сломала существующий (125→152/1,
  `run_part2_rebuild_accumulator` вместо 10 давал 8) — задокументировано
  как часть процесса, а не как отдельный принятый выпуск. GREEN после
  id-дедупа — 153/0, включая все сценарии четвёртого corrective без единой
  правки их ожидаемых чисел.

  **Шестой corrective (независимое ревью, P2 discussion_r3872698356,
  27.08.2026): пятый заход дедуплицировал корректно, но никогда не убирал
  за собой transient-буфер.** `_commit_skip_ids` переносил id из
  `sales_docs_skipped_store_ids` (пишет `_collect_sales`) в durable
  `sales_docs_skipped_store_committed_ids`, но НИКОГДА не очищал сам
  transient-буфер — ни когда переносил новые id, ни (тем более) на
  no-new-ids пути. Раз `_collect_sales` только дописывает в него
  (`setdefault(...).append(...)`) и сам никогда не читает и не чистит, без
  явной очистки буфер рос БЕЗ ГРАНИЦ на каждом дальнейшем чанке одной
  пересборки — крупный аккаунт с большим числом пропущенных документов
  дублировал бы обе (растущие) копии id в КАЖДОМ `_persist`, раздувая
  `stats_json` и owner-only `/api/sync/status`, который Настройки поллят
  каждые 1–2 секунды.

  Правка — одна строка по существу: `_commit_skip_ids` теперь ЗАБИРАЕТ
  transient-буфер (`stats.pop(_SKIP_DOC_IDS_KEY, None)`, а не
  `stats.get(...)`) в самом начале, ДО обеих веток (`not found` и
  `not new_ids`) — оба ранних возврата теперь тоже проходят через уже
  выполненную очистку. Идентичность и счёт не изменились: committed_ids
  остаётся единственным durable dedup-набором, `_collect_sales` и отбор
  продаж не тронуты.

  Регрессия: RED на версии пятого corrective (`51ca4cf`) — 162 OK/6 FAIL:
  один интеграционный (реальный `/api/sync/status` после успешного
  коммита фазы month показывает `sales_docs_skipped_store_ids` рядом с
  `_committed_ids`, обе копии по 8 id) и пять прямых юнит-проверок
  `_commit_skip_ids` (буфер не удалён ни на пути с новыми id, ни на
  no-new-ids пути, ни через серию из трёх последовательных коммитов).
  GREEN после очистки — 168/0, включая доказательство, что дубликаты
  по-прежнему не задваивают счёт, а генуинно новые id по-прежнему
  засчитываются ровно один раз.

  **Седьмой corrective (независимое ревью, P2 discussion_r3872928035,
  27.08.2026): шестой заход дедуплицировал и чистил буфер правильно, но
  делал это ПОЗЖЕ, чем нужно — коммит id срабатывал ПОСЛЕ публикации
  resume boundary, а не до неё.** `_commit_skip_ids` вызывался из
  `_run_initial` уже после `_finalize_lite`/после общего `_persist`
  today-фазы — а именно эти вызовы публикуют `history_loaded_from`,
  `history_loaded_to`, `window_done`, после которой следующий resume
  больше НЕ перечитает соответствующий диапазон. Прогон, убитый МЕЖДУ
  реальной публикацией границы (сетевой персист уже произошёл) и коммитом
  id, оставлял snapshot с положительным raw и непотреблёнными transient
  id, но `sales_docs_skipped_store_committed` так и оставался 0 (поле
  присутствует — инициализировано в начале `_run_initial`, — поэтому
  fallback на raw в `_skip_segment_contribution` не срабатывал): следующий
  resume пропускал уже закрытый диапазон и НАВСЕГДА терял находку.

  Правка — чисто упорядочивание вызовов, без изменения того, ЧТО
  коммитится: `_commit_skip_ids` теперь вызывается сразу после каждого
  успешного sales-коммита и строго до публикации соответствующей ему
  resume-границы — today-фаза (до `_persist(stage="today", detail="Остатки
  на сегодня обновлены")`), резюмированная heal_from месячная фаза (до
  `_finalize_lite`) и fresh месячная фаза (до своего `_finalize_lite`).
  Упорядочивание по чанкам истории уже было верным (коммит идёт сразу
  после продвижения `history_loaded_from` чанка, до персиста конца цикла)
  и не менялось.

  Регрессия: RED-тесты не ждут настоящего SIGKILL — monkeypatch на
  `_finalize_lite`/`_persist` вызывает РЕАЛЬНУЮ реализацию (граница
  ДЕЙСТВИТЕЛЬНО публикуется в БД), а сразу после её возврата поднимает
  исключение, воспроизводя момент kill сразу после network-persist. RED
  на версии шестого corrective (`1c07048`) — 187 OK/6 FAIL: оба новых
  сценария (месячное окно и резюмированная фаза today) честно показывают
  `sales_docs_skipped_store_committed == 0` сразу после публикации
  границы, протекший transient-буфер и НАВСЕГДА потерянную находку
  (`diagnostics` становится `None` после чистого резюме, вместо
  сохранённого значения). GREEN после переупорядочивания — 193/0, включая
  все сценарии correctives #1–#6 без единой правки их ожидаемых чисел
  (disjoint 8+2+0=10, overlap dedup 8 не 16, repeated-resume 8+3=11 не 14,
  transient-буфер по-прежнему чистится на каждом пути).

  **Восьмой corrective (независимое ревью, P2 discussion_r3873297171,
  27.08.2026): соседний случай той же природы, но не про упорядочивание
  внутри одного прогона (закрыто седьмым) — про ДВЕ независимые оценки
  валидности резюме на границе между прогонами.** `start_sync`/`_run_sync`
  вызывают `_carry_stats(..., resuming=True)` (перенося
  rebuild-checkpoint и committed-id набор упавшего сегмента) на одном лишь
  факте, что `_pending_resume` вернул точку продолжения. `_run_initial`
  же ПОСЛЕ этого независимо проверяет `_has_stock_rows(org_id)` и
  `prev_fp == fingerprint` и может отвергнуть резюме, уйдя веткой fresh
  (полная пересборка с нуля). До фикса стухшая бухгалтерия отвергнутого
  резюме не снималась: честная авторитетная пересборка находила
  raw/committed=0, но `_rebuild_total` всё равно прибавлял стухший
  checkpoint — sticky никогда не очищался в 0 (пример независимого
  ревью: «5 remains after finding zero»), хотя пересборка честно нашла
  ноль пропусков с самого начала.

  Правка — в `_run_initial`, сразу после вычисления `resumed`: если
  `resume_from` был (резюме ожидалось), а `resumed` всё равно False (обе
  независимые проверки его отвергли), `sales_docs_skipped_store_
  rebuild_checkpoint` и `sales_docs_skipped_store_committed_ids` явно
  снимаются ДО начала fresh-ветки. Прежний sticky при этом НЕ трогается —
  он остаётся видимым владельцу до успешной финализации именно этого
  прогона, как и раньше (валидное резюме, упорядочивание коммитов,
  identity-дедуп, transient pop и инкрементная семантика не менялись).

  Регрессия: два новых интеграционных сценария изолируют обе независимые
  причины отказа резюме по отдельности — раунд 1 в обоих сеет 8 skip-
  документов и честно падает в фазе history (committed=8,
  checkpoint-eligible для раунда 2); перед раундом 2 сценарий A
  (`stock_days` пуст) удаляет строки таблицы `stock_days` организации
  напрямую, сценарий B (`resume_fp` не совпадает) правит `resume_fp` в
  `sync_state.stats_json` напрямую на заведомо несовпадающее значение —
  оба честно воспроизводят операционные пути, которыми `_has_stock_rows`/
  `prev_fp` расходятся с `_pending_resume`, не трогая штатный экран выбора
  складов (тот сам снимает точку продолжения через `clear_resume_point`).
  RED на версии седьмого corrective (`a042bd2`) — 217 OK/2 FAIL: оба
  сценария честно показывают `diagnostics == 8` (стухший checkpoint)
  вместо ожидаемого авторитетного 0 после честного находящего ноль
  пересканирования. GREEN после явной очистки — 219/0, включая все
  сценарии correctives #1–#7 без единой правки их ожидаемых чисел.

  **Девятый corrective (независимое ревью, P2 discussion_r3873573203,
  27.08.2026): crash-consistency щель на fresh-ветке месячной фазы —
  ДО коммита остатков+продаж месяца evidence уже теряется НАВСЕГДА, а не
  просто остаётся стухшей (как в предыдущих correctives).** `_write_stock_
  rows` (реальный DB-коммит остатков+продаж месяца) уже состоялся, но
  `_sales_written` следом делает СОБСТВЕННЫЙ ПРЯМОЙ `_set_state(stats_
  json=...)` — в обход `_persist` — а дальше идёт сетевой `_sync_incoming`.
  До фикса `_commit_skip_ids`/`_finalize_lite` откладывались до ПОСЛЕ
  `_sync_incoming`: снимок `_sales_written` был ПЕРВЫМ, где новая граница
  (`history_loaded_from=w_start`) уже опубликована, а committed для
  найденных в этом месяце пропусков — ещё нет. При резюме в тот же день
  эту щель маскировал widened-reread фазы history (corrective #5/#7,
  `remaining` считается от «застрявшей» границы и попутно перечитывает
  пропущенное) — но при разрыве ДОЛЬШЕ HISTORY_DAYS «точка старше окна»
  (ревью 21.08, минор 7) схлопывает `resume_from` к `oldest`, решив «всё
  окно уже загружено»: `heal_from` от `oldest` не достаёт до `w_start` того
  месяца — находка терялась НАВСЕГДА. Хуже того, sticky при этом падает не
  в стухшее число, а в `None` (полностью «забывается»): `_carry_stats(
  resuming=True)` строит carry из committed/checkpoint предыдущего
  сегмента, а НЕ из bootstrap-эвристики голого raw, которой пользуется
  только owner-facing `get_status`/`_resolve_skip_diagnostic` — тот и
  показывал обманчивые «8» сразу после падения, маскируя, что дальше
  нести это число некому.

  Правка — чисто переупорядочивание: `_commit_skip_ids(stats)` теперь
  вызывается СРАЗУ после `_write_stock_rows`, ДО `_sales_written` (а не
  после `_sync_incoming`, как раньше) — граница (`history_loaded_from`) и
  связанные счётчики окна тоже выставляются ДО `_sales_written`. Любой
  snapshot начиная с публикации новой границы теперь несёт вместе с ней и
  уже подтверждённый committed — независимо от того, когда и через какой
  разрыв случится следующий resume. Ветка resumed heal_from месяца
  отдельно проверена: там граница (`history_loaded_from=resume_from`)
  публикуется ЕЩЁ ДО начала heal_from-догона (в фазе today), поэтому
  аналогичного рассинхрона нет — падение в её собственном `_sync_incoming`
  просто заставляет следующий resume честно повторить тот же heal_from
  диапазон заново, без потери.

  Регрессия: RED-тест воспроизводит НАСТОЯЩИЙ kill, а не мягкое
  исключение — поднимает класс, унаследованный от `BaseException`
  НАПРЯМУЮ (не от `Exception`), который проходит мимо ВСЕХ `except
  Exception` по стеку синка (включая честный persist самого `_run_sync`
  при обычном исключении) — фоновый поток умирает без единой строки
  cleanup-кода. Восстановление — `reset_stale_running()`, тот же путь,
  которым продакшн поднимает зависший `running` после рестарта процесса
  (`tests/test_startup_lifecycle.py`). Многодневный разрыв перед резюме
  смоделирован штатным тестовым швом `ms_sync._today` (тот же приём, что
  и в `tests/test_sync.py`, сценарии p7/p11). RED на версии восьмого
  corrective (`e98c800`) — 228 OK/4 FAIL: `history_loaded_from` застрял на
  дне падения вместо `w_start`, `committed == 0` вместо 8, transient
  id-буфер протёк, а после резюме через разрыв sticky упал в `None`
  (буквальная потеря, не просто стухшее число). GREEN после
  переупорядочивания — 232/0, включая все сценарии correctives #1–#8 без
  единой правки их ожидаемых чисел; `tests/test_sync.py` (414/0, включая
  собственные многодневные сценарии p7/p11) без единого движения.

  **Десятый corrective (независимое ревью, P2 discussion_r3873996582,
  27.08.2026): побочный эффект самого фикса девятого corrective —
  owner-facing счётчики продаж/возвратов месяца стали stale.** Перенос
  `history_loaded_from`/`_commit_skip_ids` ДО `_sales_written` (девятый
  corrective) заодно утащил за собой и снятие `window_sales_rows`/
  `window_return_rows` — счётчики месяца в панели «Подробнее»
  (`_stages_out`, ключи `sales_rows`/`return_rows` этапа `month`) — а
  инкрементирует их именно `_sales_written` (`stats["sales_rows"] +=`/
  `stats["return_rows"] +=`), НЕ `_collect_sales`. Раз снимок теперь
  делается ДО `_sales_written`, оба поля стали stale (обычно 0) сразу
  после честного fresh-синка с ненулевыми продажами и возвратами месяца.
  `window_sales_docs` эту болезнь не разделяет: его инкрементирует сам
  `_collect_sales`, отработавший ДО снимка — его позиция не менялась.

  Правка — минимальное разделение момента снятия: `window_sales_docs`
  остаётся на прежнем месте (ДО `_sales_written`, вместе с
  `history_loaded_from`/`coverage_days`/`_commit_skip_ids` — crash-safety
  девятого corrective не переоткрывается), а `window_sales_rows`/
  `window_return_rows` теперь читаются СРАЗУ ПОСЛЕ `_sales_written`.

  Регрессия: обычный fresh-синк без фолтов/подмен (дефолтные мок-данные
  месяца уже содержат и продажи, и возвраты) — сравнение `stages[month].
  counts.sales_rows/return_rows` из `/api/sync/status` с фактическим
  числом строк `sales` за окно, посчитанным напрямую по БД (окно
  публикуется `replace_all=True` один раз и больше в этом прогоне не
  трогается — история идёт старше окна, `replace_all=False`). RED на
  версии девятого corrective (`70bff44`) — 238 OK/2 FAIL: `sales_rows=0`
  вместо фактических 40, `return_rows=0` вместо фактического 1, при
  корректном `sales_docs=12`. GREEN после разделения — 240/0, включая
  hard-kill сценарий девятого corrective и все сценарии correctives
  #1–#9 без единой правки их ожидаемых чисел.
* ~~ошибка справочника контрагентов **затирает всех поставщиков** пустыми
  значениями — после такого синка правило распределения по производствам
  перестаёт работать.~~ **Закрыто.** `_upsert_products` писал
  `row.supplier` безусловно на каждом синке, а сбой чтения справочника
  (`fetch_counterparties()`) заранее подменял кэш `_SUPPLIERS[org_id]` на
  `{}` и глотал исключение — итог был неотличим от «МойСклад авторитетно
  подтвердил, что поставщика нет». Теперь `stats["suppliers_error"]`
  (уже писался, но раньше ни на что не влиял) блокирует запись поля в этом
  прогоне: у существующих товаров остаётся последнее подтверждённое имя, у
  новых — прежний дефолт колонки. Успешный ответ справочника — в том числе
  авторитетно пустой или без нужной привязки — по-прежнему обнуляет
  `supplier`, поведение не изменилось.

  **Второй заход (найдено независимым ревью, discussion_r3865732915):
  первая версия гейта смешивала два разных «не знаем».** Отсутствие ссылки
  на поставщика у товара — это факт, известный из самого ассортимента, БЕЗ
  участия справочника контрагентов; только НЕразрешённая (но присутствующая)
  ссылка — это сигнал, зависящий от справочника. Первая версия гейта
  блокировала запись поля целиком при сбое справочника — и заодно с
  сохранением непрочитавшейся ссылки ошибочно сохраняла и устаревшее имя у
  товара, у которого ссылку сняли в этом же прогоне. Теперь `_parse_assortment`
  прокидывает в `_upsert_products` отдельный флаг присутствия ссылки
  (`supplier_link`, независимый от резолва в имя через `_SUPPLIERS`); при
  сбое справочника поле обнуляется, если ссылки нет вовсе, и сохраняется,
  только если ссылка есть, но не резолвится. Регрессия —
  `tests/test_sync_supplier.py` (33 проверки: RED на прежнем коде — снятие
  ссылки одновременно со сбоем справочника не обнуляло `supplier`; GREEN
  проверяет также валидное обновление, сбой при присутствующей ссылке,
  авторитетный пустой ответ, сходимость после восстановления и изоляцию
  организаций — в обоих заходах).

Все три сценария закрыты 27.08.2026 — DATA-8 закрыт целиком.

### DATA-9. `base_name` как ключ пользовательских данных
**[подтверждено]** · влияние: среднее, структурное · стоимость: неделя+

Имя товара — не идентификатор. По нему ключуются «едет к нам», ручные скидки,
архив, привязка к производству, черновики ростовки. Два разных товара с одинаковым
именем схлопнутся в одну строку. Перенос данных при переименовании работает
**только в однозначном случае** (все размеры переехали на одно новое имя);
частичное переименование оставляет осиротевшие записи, наружу — счётчик.

### ~~DATA-10. Опечатка в выбранном типе цены обнуляет себестоимость~~
**закрыто 24.08.2026** (было: подтверждено · влияние среднее · стоимость час)

Формулировка описывала половину дыры. Проверено кодом и тестом: обнулением
себестоимости дело не ограничивалось. Если организация выбрала тип цены, а потом
переименовала его в МойСкладе (или опечаталась при выборе), точный поиск по
имени не откатывался на подсказки — и дальше расходились две беды. Полная
себестоимость записывалась нулём по всему ассортименту, деньги пересчитывались
по закупочной цене (у бренда со своим пошивом это стоимость пошива без ткани —
разница в разы). А цена продажи откатывалась на «первую цену в списке», то есть
на ЧУЖОЙ тип: в тесте после удаления типа «Цена продажи» в карточку приезжала
цена себестоимости, 3600 ₽ вместо 5900 ₽. Это хуже нуля — ноль видно,
подменённую цену нет. Ни предупреждения, ни алерта: синк заканчивался словом
«Синхронизация завершена».

Закрыто фейлом закрытым: непустой явно выбранный тип, отсутствующий во всём
ассортименте, останавливает синк до записи товаров, обе прежние цены остаются в
базе, а актуальный список типов доезжает в настройки, чтобы выбор можно было
исправить. Границы правила (частичное отсутствие, пустая настройка, одинаковое
поведение продажи и себестоимости) — в DECISIONS D-40. Регрессия —
`tests/test_price_types.py`, 72 проверки, плюс 7 проверок алерта в
`tests/test_notify.py`.

Считается «есть ли выбранный тип» только по строкам, которые синк реально
импортирует (`product`/`variant`). Первая версия замка собирала типы цен со
ВСЕХ строк ответа МойСклада, а услуги, комплекты и серии в products не попадают
вовсе — тип, оставшийся у услуги, засчитывался как присутствующий, и замок
молчал ровно в том случае, ради которого написан: у импортируемого товара цена
продажи откатывалась на чужой тип, а `cost_full` записывался нулём. Найдено
внешним ревью 25.08.2026 на PR #10, воспроизведено и закрыто там же; список
импортируемых типов строк в коде теперь один на оба места, разъехаться им
больше нечем.

Ещё три прохода того же ревью, найденные и закрытые на PR #10 25.08.2026.

**Подмена типа на ОТДЕЛЬНОЙ карточке.** Замок сверяет глобальное отсутствие, и
это сознательная граница — частичное отсутствие остаётся нормой каталога. Но в
этой норме `_sale_price_of` доходил до «первой цены в списке» и записывал
карточке чужой тип: у товара, где выбранной цены продажи нет, а полная
себестоимость есть, первой оказывалась именно она. Дыра уже исходной (портятся
отдельные карточки, а не весь ассортимент) и тише: замок молчит законно, синк
рапортует «завершена». Закрыто так: явный выбор больше не откатывается ни на
что — отсутствие представляется нулём. На до-фиксовом коде проверки давали
3600 ₽ у `p-bag1` и 5850 ₽ у модификации `v-hoodie1-M` вместо 0 ₽ и
унаследованных от родителя 9800 ₽.

**Подсказка, обещавшая не то.** Остановка поднималась голым `RuntimeError`,
`error_cause()` относил её к `internal`, и Telegram-алерт дописывал «Мы уже
разбираемся» поверх текста, который зовёт владельца в Настройки; полоска синка
по той же причине предлагала «Повторить», хотя повтор без смены настройки даёт
ту же ошибку. Закрыто отдельным классом `PriceTypesGone` и отдельной причиной
`settings`. Переклассификации скопом нет: посторонний `RuntimeError` остаётся
`internal`, и это под проверкой.

**Остановка без выхода.** Замок всегда сверял ПОЛНЫЙ список, а до владельца
доезжал обрезанный: в состояние сохранялись первые 20 имён, ручка настроек
отдавала первые 40, а `<select>` на экране закрытый — чего в нём нет, того
выбрать нельзя. На аккаунте с большим числом типов цен годная замена
оказывалась за границей обрезки, и владелец был заперт: синк требует поменять
настройку, настройка не предлагает чем. Обе обрезки сняты — снять одну значило
бы перенести тупик с двадцатой позиции на сороковую. На до-фиксовом коде
проверка получала 20 имён вместо 46, последним — «Тип цены 19». Цена решения:
`stats` и ответ ручки растут вместе с числом типов цен в аккаунте; это
справочник уровня аккаунта, и полный список всё равно уже считается для замка.

### DATA-11. Расхождения формул между модулями
**[подтверждено]** · влияние: среднее · стоимость: 2–3 дня

Развёрнуто в BUSINESS_LOGIC §9. Кратко: потребность считается двумя формулами;
срок производства и канал определяются тремя способами; минимальных партий две
на одной сущности; настройка «окно темпа» действует не на всех страницах;
мастер игнорирует ручные правки ростовки. Каждое расхождение по отдельности
выглядит мелочью, вместе они означают, что **две страницы продукта могут дать
разные ответы на один вопрос**, и объяснить это клиенту нечем.

---

## B. Безопасность и мультитенантность

### SEC-1. Утечка настроек чужой организации через `production_id` в брифе — ЗАКРЫТО
**[закрыто, воспроизведено тестом]** · было: влияние высокое · стоимость: час

*Закрыто.* `app/order_planner.py` при расчёте плана не просто отказывается
использовать чужое производство, а **затирает** значение в брифе
(`brief["production_id"] = None`), поэтому применение плана не может перенести
чужой идентификатор в заказ. Путь пройден целиком и воспроизведён:
`tests/test_isolation.py` §4 — бриф с чужим `production_id` → сохранение →
`apply` → заказ → календарь платежей; в заказе чужого идентификатора нет.
Проверено на `a8feeb51` 24.08.2026, до этой ветки: правка была сделана раньше,
а текст долга просто не обновили. Ниже — описание в прежней редакции, оно
остаётся точным описанием того, что было.

Единственное во всём коде получение производства по идентификатору **без проверки
организации** находится в сборке этапов заказа. Путь туда существует:
нормализация брифа копирует присланный `production_id` как есть; расчёт плана
проверяет принадлежность организации и при несовпадении не использует
производство, **но не затирает значение в брифе**; бриф сохраняется; применение
плана пишет этот идентификатор в заказ без повторной проверки. Дальше настройки
чужих этапов (сроки, доли себестоимости, размеры предоплат) попадают в календарь
платежей.

Это не утечка товарных данных, но это утечка коммерческих условий чужого
подрядчика. **Проверить в первую очередь.**

### SEC-2. Непоследовательная проверки роли
**[подтверждено]** · влияние: среднее · стоимость: полдня

Часть ручек, меняющих данные всей организации, доступна роли «участник»:
архивирование позиций, слияние и перенос категорий, черновики ростовки, создание
и удаление заказов, применение плана — при том что соседние ручки, меняющие те же
сущности, требуют владельца. Правила, по которому одно owner-only, а другое нет,
я не нашёл.

*Обновление на 0fd3783:* один пункт из этого списка закрыт — настройка этапов
производства стала owner-only. Остальные остаются.

### SEC-3. Сессию нельзя отозвать — ЗАКРЫТО
**[закрыто, воспроизведено тестом]** · было: влияние среднее · стоимость: 1 день

Было: серверного хранилища сессий нет, смена пароля не разлогинивала другие
устройства (до 7 дней). В интерфейсе это было написано честно, но для B2B это
плохой ответ.

*Закрыто 27.08.2026.* Серверное хранилище сессий по-прежнему не заводили (это
и не требовалось) — вместо этого у `users` появилась аддитивная монотонная
`session_version` (`INTEGER NOT NULL DEFAULT 0`; миграция
`users_session_version_v1` в `app/models.py` — `ensure_schema` →
`_ensure_users_session_version`, старые базы получают колонку ALTER'ом, новые
— прямо из модели). Подписанная сессионная кука несёт версию на момент
выдачи; `auth.resolve_auth` сравнивает её с текущей записью пользователя и
отказывает при несовпадении. Кука довыпущенного формата — без поля версии —
трактуется как версия 0 и остаётся рабочей, пока в базе тоже 0: сам факт
выката этой миграции никого не разлогинивает. Нечисловое значение версии
(строка, дробь, `bool`) отклоняется fail-closed, а не приравнивается к 0.

Смена пароля (`POST /api/account/password`) одной транзакцией поднимает
`pw_hash` и `session_version` и сразу перевыпускает куку текущему клиенту
новой версией — свою же сессию смена пароля не обрывает. Все прочие ранее
выданные сессии этого пользователя (обычный вход, регистрация и вход из
iframe МойСклад через `app/routes_ms_app.py`) получают 401 (API) или редирект
на `/login` (страницы) на следующий запрос — принудительно, а не когда
протухнет 7-дневная кука. Сессии других пользователей не затрагиваются.
Интерфейс (`templates/account.html`) больше не утверждает, что чужой вход
держится до 7 дней — сказано, что он завершается немедленно.

Доказано `tests/test_account.py`, разделы «Гашение сессий после смены
пароля», «Кука без версии... и битая версия» и «Аддитивная миграция
users.session_version»: два клиента одного пользователя — после смены пароля
второй получает 401/редирект, первый остаётся авторизован; сторонний
пользователь не задет; новый пароль пускает, старый — нет; кука без версии
работает при DB version 0 и отзывается сразу после первого инкремента;
строковая/дробная/булева версия отклоняется всегда; страница «Аккаунт»
больше не содержит текста про 7 дней; миграция на старой схеме `users`
аддитивна, повторный и конкурентный (несколько потоков через
`run_migration_once`) запуск безопасны.

### SEC-4. Вывод ключей — не KDF, ротации нет
**[подтверждено]** · влияние: среднее · стоимость: 1–2 дня

Разделение ключей по назначению сделано правильно, но сам вывод — одинарный
SHA-256 без соли и растяжения, энтропия секрета не проверяется. Ротация не
поддержана: смена мастер-секрета делает все сохранённые токены МойСклада
нечитаемыми, и этот сценарий нигде не обрабатывается.

*Частично смягчено 23.08:* учение по восстановлению
(`deploy/offsite_restore_drill.sh`) расшифровывает сохранённые токены копией
секрета и падает, если она не подходит. Саму ротацию это не поддерживает, но
расхождение «секрет сменили — копию не обновили» теперь обнаруживается
еженедельно, а не в день аварии.

### SEC-5. Формы входа/регистрации не покрыты CSRF-защитой — ЗАКРЫТО
**[закрыто, воспроизведено тестом]** · было: влияние низкое-среднее · стоимость: часы

Было: защита распространялась только на пути `/api/*`. Вход, регистрация и
выход — обычные формы вне `/api`, их прикрывал только `SameSite`. Во встроенном
режиме на проде кука ставится с `SameSite=None` — то есть для этих трёх путей
защиты не оставалось. Отдельно: вся схема CSRF держится на **отсутствии**
CORS-middleware; добавление его с поддержкой учётных данных отключит защиту
молча (это остаётся верным и после закрытия пункта — см. ниже).

*Обновление 24.08.2026:* сторож `tests/test_isolation.py` §5 — падает, если
CORS-middleware появится, и проверяет отказ изменяющего запроса к `/api` без
`X-Oborot-CSRF`. Сам пункт оставался открытым: формы входа, регистрации и
выхода жили вне `/api` и защиты не имели.

*Закрыто 26.08.2026.* `/login`, `/register`, `/logout` защищены подписанным
double-submit токеном: сервер выдаёт HttpOnly-куку `oborot_csrf` (ключ подписи
— отдельный от сессионного, `crypto.get_csrf_signing_secret`) и то же значение
кладёт в скрытое поле формы; при отправке кука и поле обязаны совпасть
константным сравнением (`auth.verify_csrf_form`). Токен не перевыпускается на
каждый рендер — переживает несколько вкладок и кнопку «назад» без ложных
отказов. Во встроенном режиме на проде кука ставится `SameSite=None + Secure`,
как и сессионная. `X-Oborot-CSRF` остаётся допустимой альтернативой и здесь —
то же свойство (кросс-доменная форма не может поставить кастомный заголовок),
на котором уже стоит защита `/api`; это и сохранило совместимость машинных
клиентов существующих ~30 тестовых наборов без правки каждого — тронуть
пришлось только `tests/test_logging.py` (единственный набор, ходивший на
`/login`/`/register` без этого заголовка вовсе).

Доказано `tests/test_auth_csrf.py`: настоящая браузерная форма (без
заголовка-обхода) проходит все три пути; отсутствие токена, только кука,
только поле, несовпадение, поле из чужого браузера — отклонены ДО мутации
(пользователь не создан / сессия не снята); повторный GET не меняет токен;
встроенный режим на проде даёт `SameSite=None + Secure`. Контроль: с
`auth.verify_csrf_form`, подменённым в памяти на «всегда true» (имитация
поведения до фикса — проверка отсутствует), ровно проверки границы (разделы
2–3, 9 штук) становятся красными, остальные (реальный браузер, машинный
обход, стабильность токена) остаются зелёными — тесты действительно ловят
дефект, а не просто присутствие скрытого поля.

### SEC-6. Заказы-планы не удаляются вместе с организацией — ЗАКРЫТО
**[закрыто, воспроизведено тестом]** · было: влияние низкое · стоимость: минуты

Было: при удалении организации перечислено 20 моделей, но не планы заказов
(и не прогресс уроков с настройками пользователя). В PostgreSQL это нарушение
внешнего ключа, в SQLite — осиротевшие строки.

*Закрыто.* `_purge_org` стирает `OrderPlan` и `OrderReceipt`, `_purge_user` —
`UserLesson`, `UserPrefs`, `UserHintSeen`. Доказано реальными строками, а не
пустыми таблицами: `tests/test_account.py` заводит план заказа, пройденный урок,
тумблер подсказок и отметку инструкции у трёх разных людей и проверяет, что
следы ушедших исчезли, а следы оставшегося коллеги не задеты;
`tests/test_decision_record.py` отдельно проверяет планы. Прогон усиленного
набора против кода `a8feeb51` зелёный — правка была сделана раньше, устарел
только текст долга.

*Что появилось вместе с закрытием.* Полнота держалась на ручном списке моделей,
и ничто не связывало его с реальным набором таблиц. Теперь список объявлен в
`app/tenancy.py`, а `tests/test_tenancy.py` сверяет его с реестром SQLAlchemy:
новая модель со ссылкой на организацию или пользователя обязана быть либо в
списке удаления, либо в списке не-владеющих ссылок с причиной (D-41). Сторож же
нашёл и первую ошибку порядка — см. SEC-9.

*Границы закрытия — узкие, и это важно.* Закрыто ровно то, чем пункт был заведён:
полнота набора удаления организации и личного прогресса пользователя. Пункт НЕ
означает, что удаление пользователя вообще безопасно: ссылки `created_by` и
`billing_requests.user_id` переживают человека и сегодня, и последствия этого
записаны отдельным открытым пунктом — SEC-8.

### SEC-7. Опубликованные юридические документы не вычитаны
**[подтверждено комментарием в коде]** · влияние: зависит от юриста

Оферта и политика ПДн доступны публично в «рабочей редакции».

### SEC-8. Ссылки на пользователя переживают его удаление: заявка на счёт запрещает DELETE, авторство перепривязывается
**[подтверждено кодом и воспроизведено тестом на SQLite с `PRAGMA foreign_keys=ON`]** ·
влияние: **среднее-высокое, security-correctness** · стоимость: часы + продуктовое решение владельца

Ссылки на `users.id` из org-owned строк при удалении пользователя не зануляются:
строка принадлежит организации и уходит вместе с ней (D-41). Пока организация
жива, ссылка на удалённого человека остаётся висеть. Последствий два, и они
разного рода.

**1. `billing_requests.user_id` — `nullable=False` FK на `users.id`.** Заявку на
счёт нельзя оставить без автора, и удаление автора при живой организации:
* в PostgreSQL (заявлен в README) и в SQLite с `PRAGMA foreign_keys=ON` —
  **отклоняется внешним ключом**: человек, подававший заявку, физически не может
  удалить аккаунт, ручка `/api/account/delete` вернёт ошибку базы;
* в сегодняшнем прод-SQLite (ключи по умолчанию выключены) — **проходит и
  оставляет сироту**: заявка ссылается на несуществующий `users.id`. SQLite
  выдаёт новым строкам `rowid = max+1`, поэтому освободившийся идентификатор
  может достаться другому человеку, и заявка молча привяжется к постороннему.

Воспроизведено: `tests/test_tenancy.py`, раздел 10 — на отдельной базе с
включёнными ключами `_purge_user` автора заявки падает с
`FOREIGN KEY constraint failed`. Эта проверка — трекер долга: когда SEC-8
закроют, она обязана упасть и быть удалённой вместе с этим пунктом.

**2. `created_by` в `production_orders`, `order_plans`, `order_receipts`.**
Колонки nullable, DELETE ими не блокируется, но висячий идентификатор остаётся.
Имя автора при этом достаётся запросом
`select(User.id, User.name, User.email).where(User.id.in_(ids))` **без фильтра
по организации** (`app/api.py:306-314`, `app/api.py:2573-2581`). Вместе с
переиспользованием `rowid` это **межарендаторская подмена автора**: в списке
заказов организации A может показаться имя и почта человека из организации B.

**Чего этот долг НЕ означает.** Записи в `NON_OWNING_USER_REFERENCES`
(`app/tenancy.py`) — это исключение только из удаления org-owned строки в потоке
пользователя. Referential safety этих ссылок ими НЕ доказана и остаётся открытой
ровно этим пунктом; поэтому список и не называется allowlist.

**Почему не исправлено здесь.** Все разумные лечения — зануление `created_by`,
перепривязка заявки на владельца организации, запрет удаления до закрытия
заявок, фильтр по членству при показе автора — меняют то, **что видит
пользователь** (автор, удаливший аккаунт, перестанет называться) и требуют
миграции. Это продуктовое решение и решение о хранении заявки; принимает его
владелец. Ветка `claude/tenant-deletion-guards` рантайм и миграции не трогает
сознательно.

### SEC-9. Порядок удаления организации нарушал внешний ключ `production_id` — ЗАКРЫТО
**[закрыто, найдено сторожем]** · было: влияние низкое (на SQLite не проявлялось) · стоимость: минуты

`_purge_org` стирал `productions` **раньше** `production_orders`, у которых есть
`production_id -> productions.id`. На SQLite внешние ключи не проверяются, и
дефект не проявлялся ничем; на PostgreSQL, заявленном в README, удаление
организации упало бы. Нашёл не отчёт с прода, а проверка порядка в
`tests/test_tenancy.py`. Исправлено перестановкой в `app/tenancy.py`:
`ProductionOrder` теперь удаляется перед `Production`.

---

## C. Масштабирование и эксплуатация

### OPS-1. Один воркер обязателен, но ничем не форсируется
**[подтверждено]** · влияние: высокое · стоимость: час

В памяти процесса живут: кэш аналитики и его инвалидация, реестр потоков синка,
флаг планировщика, лимитер входа, анти-replay для внешних JWT, кэши типов цен и
поставщиков. При двух воркерах пользователь увидит разные числа на одной странице
(до 10 минут), джобы выполнятся дважды, синк одной организации может пойти
параллельно сам с собой. Соседний, менее опасный параметр защищён fail-fast'ом,
а этот — только комментарием.

*Смягчающее обстоятельство:* прод фактически запускается одним процессом через
systemd (`python run.py`). То есть сегодня требование выполняется — но держится
на строке в юните, а не на коде, и первая же попытка масштабирования его нарушит
молча.

Отдельно: сброс «зависших» синков на старте процесса объявит оборванным синк,
реально идущий в другом живом процессе.

**Дёшево и достаточно:** падать на старте при числе воркеров > 1 с включённым
планировщиком. Redis и отдельные воркеры — совсем другая задача и,
по моей оценке, сейчас преждевременная.

### OPS-2. Наблюдаемости практически нет
**[подтверждено]** · влияние: высокое · стоимость: 1 день

Конфигурации логирования нет вообще; в модуле синхронизации семь голых `print()`.
Нет health-эндпоинтов, метрик, сборщика исключений. Единственный сигнал наружу —
Telegram-алерт о втором подряд упавшем синке. Когда клиент скажет «у меня всё
сломалось», инструментов для ответа нет.

### OPS-3. Деплой без CI, без пинов, без отката
**[гипотеза по репозиторию + известно из практики проекта]** · влияние: **высокое** · стоимость: полдня

В репозитории нет ни CI, ни контейнера, ни описанной процедуры; README ссылается
на несуществующий файл деплоя, единственный конфиг развёртывания лежит в каталоге
предшественника и описывает другой сервис. Фактический процесс — ручная загрузка
файлов через веб-интерфейс GitHub, при которой загрузка файла целиком **молча
затирает чужие правки**; это уже приводило к потере работы и потребовало
отдельного merge-коммита (`a2165ae`, 22.08).

~~Все зависимости зафиксированы как `>=`, lock-файла нет~~ — **закрыто 23.08**:
`requirements.lock` фиксирует 32 точные версии (полное транзитивное замыкание
13 прямых зависимостей), и ставят его И деплой, И CI. Порознь это не работает:
если lock ставит только деплой, «зелёный CI» и «то, что поехало в бой» —
разные наборы версий, и смысл фиксации теряется наполовину. `requirements.txt`
остаётся человекочитаемым списком намерений с диапазонами.

CI (`.github/workflows/ci.yml`) и выкатка по SHA с откатом одной командой
(`deploy/deploy.sh`) добавлены раньше, 22.08. Незакрытым в этом пункте остаётся
самое неприятное: **сама заливка по-прежнему ручная**, через веб-интерфейс
GitHub, и по-прежнему может затереть чужие правки.

*Исправлено 23.08 по внешнему ревью, три дефекта самого скрипта:*
«откат без сети» не работал вовсе — проверка отложенного окружения дёргала
консольную обёртку `bin/pip`, которая после переименования каталога не
запускается, и откат каждый раз молча уходил в пересборку через сеть (теперь
везде `python -m pip`, а окружение проверяется на финальном пути);
предварительная проверка пропускала **неотслеживаемые** файлы, которые
`git checkout` не трогает, — такой файл переживал любую выкладку, а скрипт
уверенно печатал, что в бою ровно этот коммит;
сбой между переключением кода и перезапуском оставлял прод в состоянии, которого
нет ни в одном релизе, и при этом ЖИВЫМ — теперь в этом окне выполняется полный
откат (D-46).

**По моей оценке это риск номер один в проекте** — не потому, что он самый
вероятный, а потому что он единственный, который блокирует лечение всех остальных:
крупный рефакторинг через такой пайплайн опаснее самого долга.

### OPS-4. Бэкап — ОТКРЫТ. Копия ни разу не покидала машину
**[локальная часть проверена исполнением 23.08; удалённая написана и проверена на подставном restic, на сервере не прогонялась]** · влияние: **высокое**

Формулировка «закрыто» здесь была преждевременной, и это поправка по решению
владельца от 23.08.2026. Копия, которая лежит на том же диске, что и база, —
это защита от плохого релиза и от собственной ошибки, но не от потери машины.
Пока данные существуют в одном физическом месте, бэкапа нет, сколько бы
скриптов вокруг него ни стояло.

**Что сделано и проверено исполнением** (`deploy/backup.sh`,
`deploy/restore_test.sh`, `tests/test_backup.py` — 21 проверка): согласованный
снимок работающей базы через `sqlite3 .backup`, проверка целостности и
содержимого ДО попадания копии в ротацию, ротация с retention, разворачивание
копии в отдельный файл и подъём приложения на ней, включая копию со СТАРОЙ
схемой.

**Что добавлено 23.08 вечером** (`deploy/offsite_backup.sh`,
`deploy/offsite_restore_drill.sh`, `deploy/check_restored_secrets.py`,
`tests/test_offsite.py` — 127 проверок, плюс 4 там, где тесту доступна
вторая файловая система, таймеры systemd): зашифрованная копия в
удалённое хранилище на restic; отказ работать с локальным путём вместо
удалённого; отказ выгружать базу без организаций; проверка хранилища ДО ротации;
учение, которое скачивает снимок ИЗ хранилища, проверяет данные
(`check --read-data`), убеждается, что в базе есть строки, расшифровывает
сохранённые токены интеграции отдельной офсайт-копией `OBOROT_SECRET` и
поднимает на ней приложение.

Идея решения и три его ключевые детали — из ветки `codex/offsite-backup-restore`
(атрибуция в шапке скриптов). Реализация и тесты свои.

**Найдено внешним ревью 23.08 и исправлено** (все три — «успех, которого не
должно было быть», и ни одно не видно по зелёному отчёту):

* **учение не доказывало восстановление доступа.** Токены МойСклада зашифрованы
  ключом из `OBOROT_SECRET`, которого в бэкапе нет и быть не должно. База, все
  токены которой нечитаемы, проходит и `integrity_check`, и проверку строк, и
  подъём приложения: система поднимается, показывает старые цифры и не может ни
  синхронизироваться, ни записать заказ. Теперь учение расшифровывает настоящий
  токен копией секрета, поданной ИЗВНЕ, и без неё не запускается (D-45);
* **ротация шла до проверки хранилища** (`backup → forget --prune → check`) —
  повреждение обнаруживалось после того, как старые снимки срезаны;
* **`TimeoutStartSec` у юнита копии не был задан:** `Type=oneshot` живёт с
  90-секундным умолчанием, и медленная загрузка была бы убита systemd.

**Найдено повторным внешним ревью 23.08 и исправлено** (тот же класс — успех,
которого не должно было быть):

* **проверялось «снимок есть», а не «появился новый».** Условие выполняет и
  вчерашний снимок: загрузка вернула ноль, ничего не создала, `check` подтвердил
  целость старых данных, а ротация срезала копии позавчерашние. День за днём —
  один устаревающий снимок при зелёном отчёте. Теперь id последнего снимка
  снимается до и после загрузки и обязан измениться (D-43);
* **схема хранения копии `OBOROT_SECRET` противоречила сама себе:** копия
  называлась офсайтовой, а рецепт и значение по умолчанию указывали на
  `/opt/oborot/recovery-secret` — файл на диске сервера. Учение с ним доказывало
  только то, что сервер знает свой собственный ключ. Теперь копия подаётся
  извне, скрипт отвергает файл на той же файловой системе, что и база, а юнит
  учения требует смонтированного каталога (D-47);
* **два дефекта выкладки, оба оставлявшие откат без окружения:** отметка
  «окружение подменено» ставилась после возврата из функции (падение последнего
  переименования → прод с прежним кодом и новыми библиотеками), и уборка старых
  окружений шла до первого перезапуска, не исключая цель отката, у которой
  `mtime` самый старый по построению (D-44, D-46).

**Что НЕ сделано и держит пункт открытым:**

1. **настоящий прогон на сервере с внешним провайдером.** Хранилища нет: адрес
   и доступ — за владельцем. Всё, что проверено, проверено на **подставном**
   restic;
2. **копия ни разу не уезжала** в настоящее удалённое хранилище;
3. **копия ни разу не скачивалась обратно** оттуда;
4. **приложение ни разу не поднималось** на базе, восстановленной из
   удалённого снимка;
5. **офсайт-копия секрета ни разу не проверялась в настоящей аварийной
   процедуре** — только на подставной площадке.

Отдельно про то, чего проверка адреса НЕ доказывает: запрет локального пути
отсекает самую частую ошибку, но префикс `sftp:` или `rclone:` — это способ
разговора с хранилищем, а не доказательство отдельного домена отказа.
`sftp:localhost:`, соседний контейнер того же гипервизора, локально
смонтированный каталог через `rclone:`, S3-эндпоинт в том же датацентре — всё
это проверку проходит и в день аварии умирает вместе с боевой машиной.

Ровно то же — про копию `OBOROT_SECRET`. Скрипт умеет отличить «другая файловая
система» от «файл рядом с базой» и отвергает второе, а юнит учения не стартует
без смонтированного каталога. Но что смонтированный каталог живёт в другом
физическом месте, ни скрипт, ни юнит подтвердить не могут. Домен отказа для
ключа выбирает человек — как и для самого хранилища.

**Где офсайт проверяется, а где нет** (24.08.2026, после слияния со строгим
раннером). `tests/test_offsite.py` гоняет настоящие bash-скрипты и требует
девять утилит, из которых на macOS без brew-пакета `util-linux` нет `flock`.
В таком окружении набор выходит с кодом 77 и называет недостающие утилиты
поимённо — это честный пропуск, а не зелёный результат. В CI на ubuntu утилиты
есть, набор обязателен и под `--require-all` пропуск считается падением: 127
проверок исполняются по-настоящему. Что это НЕ доказывает — сказано ниже и
остаётся в силе: restic в наборе подставной.

Закрыть пункт может только один день: копия уехала к настоящему внешнему
провайдеру, скачалась обратно, токены расшифровались офсайт-копией секрета,
приложение на ней поднялось — на настоящем сервере. Код к этому дню готов;
сам день не наступил. Ни зелёный CI, ни 127 проверок на подставном restic
этого дня не заменяют и доказательством физического off-site не являются.

**Что нужно от владельца:** любой второй адрес с ssh или S3-совместимым
доступом. Второй сервер покупать не обязательно — подойдёт домашняя машина
(`sftp:`). Требование одно, и проверить его может только человек: это не тот
диск, не та машина и не тот датацентр, где лежит база. Плюс место вне сервера
для двух секретов — пароля restic и копии `OBOROT_SECRET`.

### OPS-5. Миграции без журнала и порядка
**[подтверждено]** · влияние: среднее · стоимость: 1–2 дня

DDL размазан по шести модулям; «уже применено» определяется по подстроке в тексте
ошибки СУБД; журнал ведут только две миграции из всех. Состояние схемы боевой базы
восстанавливается только интроспекцией. Пока изменения аддитивные — работает;
первая же неаддитивная миграция (переименование, бэкфилл с логикой, удаление)
потребует настоящего инструмента.

### OPS-6. Мелкие эксплуатационные ловушки
**[подтверждено]** · влияние: низкое · частично закрыто, последняя правка 27.08.2026

**Закрыто 27.08.2026.** «Планировщик стартует до миграций» — `scheduler.attach(app)`
теперь вешает только shutdown-хендлер; `scheduler.start()` вызывается последней
строкой `app.main._startup()`, после того как все шаги старта (init_db и восемь
`ensure_schema`/`reset_stale_running`/`log_preview`) завершились успешно. Сбой
на любом из этих девяти шагов не даёт `_startup` дойти до `start()` — планировщик
не поднимается вовсе. `tests/test_startup_lifecycle.py` (набор `startup` в
`run_all.py`) проверяет реальный порядок всех девяти шагов, инъекцию сбоя в
каждый из них по отдельности и идемпотентность повторного `shutdown()`.

**Закрыто 27.08.2026 (env-парсинг синка).** Две переменные окружения синка,
`HISTORY_DAYS` и `SYNC_DAYS_BACK` (`app/ms_sync.py`), читались голым
`int(os.environ.get(...))`: мусорное значение роняло импорт модуля
исключением, а `0`/отрицательное значение проходило как есть и портило окно
истории и инкремента. Обе переведены на уже существующий `app/ms_client._env_int`
(терпимость к пробелам и мусору, откат на дефолт 730/3, нижняя граница 1) — тот
же контракт, что у остальных `_env_int`-переменных клиента МойСклада.
`tests/test_sync_env_parsing.py` (набор `sync_env` в `run_all.py`) проверяет
обе переменные в чистом дочернем процессе: unset-дефолты, валидные значения,
пробелы вокруг значения, мусор, ноль и отрицательное — итого 18 проверок против
5 FAIL на прежнем коде (мусор ронял импорт, ноль/отрицательное не клэмпились).

**Закрыто 27.08.2026 (последняя migration-on-import).** `lessons.ensure_schema()`
(таблицы `user_lessons`/`user_prefs`) вызывался прямо на импорте `app/api.py` —
до старта приложения и вне защиты от гонки нескольких воркеров, тем же классом
дефекта, что уже закрытые `ms_writeback`/`ms_vendor` (Д4). Вызов убран из
`app/api.py`; `lessons.ensure_schema()` стал девятым шагом `app.main._startup()`,
сразу после `init_db()` — рядом с прочими аддитивными миграциями и раньше
`scheduler.start()`. `tests/test_startup_lifecycle.py` расширен: реальный
порядок девяти шагов (lessons — немедленно после init_db, ровно один вызов),
инъекция сбоя в lessons.ensure_schema не даёт выполниться ни одному следующему
шагу и не даёт стартовать планировщику, а отдельная проверка в чистом дочернем
процессе доказывает, что сам `import app.api` / `import app.main` (без ASGI-цикла)
больше не мутирует схему. RED на прежнем коде — 42 OK / 7 FAIL (lessons вызывался
до init_db, инъекция сбоя в lessons не останавливала старт, импорт мутировал
схему); GREEN после правки — 49 OK / 0 FAIL.

Из этого пункта остаются открытыми: `utcnow()` (deprecated) — 12 вызовов;
`on_event` — два.

**Закрыто отдельно, PR #30 (5d2cb85, de545db).** Два семафора клиента
МойСклада не имели общего потолка (теоретически 8 запросов при лимите 5) —
исправлено до этого раунда, к текущим изменениям отношения не имеет.

### OPS-7. Проверки, которых не было, засчитывались как пройденные
**[подтверждено прогоном CI 32716761631 и воспроизведено на фиктивных наборах]**
· влияние: высокое · закрыто частично 24.08.2026

**Что было.** Раннер `tests/run_all.py` считал набор хорошим по правилу «код
возврата 0 и в отчёте нет FAIL». Набор, не выполнивший ни одной проверки, под
это правило попадал: `test_ui.py` возвращал 0, не найдя playwright,
`test_backup.py` и `test_deploy.py` печатали «ИТОГО: 0 OK, 0 FAIL» при
отсутствии sqlite3 и flock. В прогоне CI 32716761631 набор **ui показал 0/0
при зелёном итоге всего прогона**: браузерных проверок не было ни одной, и CI
об этом молчал. Отдельно: `test_turnover_canon.py` печатал итог словами без
чисел, четыре набора пишут «Итого» вместо «ИТОГО» — их числа раннер не читал
и откатывался на подсчёт строк.

**Что закрыто 24.08.2026** (D-42): приговор выносит `classify()` — PASS только
при числовом отчёте с ok > 0, SKIP только при коде 77 И маркере
«ПРОПУЩЕНО: <причина>» одновременно, тишина/0-0/нет отчёта/таймаут — падение;
CI гоняет `--require-all` без единого `--allow-skip`, playwright и Chromium
зафиксированы `requirements-dev.lock`, зашитый путь `/opt/pw-browsers` убран.
Контракт проверяется `tests/test_runner.py` (48 проверок на фиктивных
наборах), а сам он — `tests/check_runner_mutations.py`: одиннадцать мутаций
раннера, каждая обязана его уронить.

**Что осталось открытым.** Наборы `test_deploy.py` и `test_backup.py` гоняют
настоящие bash-скрипты `deploy/` и требуют `flock`, GNU `find -printf`, GNU
`stat` — на macOS без brew-пакетов `util-linux`/`findutils`/`coreutils` их
нет. Теперь это ЧЕСТНЫЙ пропуск с причиной (локально), а в CI на ubuntu оба
набора обязательны. Но переносимость самих прод-скриптов не чинилась: это
правка в `deploy/`, и смешивать её с правкой тестов нельзя — сейчас на macOS
без brew-утилит деплойные скрипты просто не проверяются никем.

Ещё не закрыто: `ui` проверяется только на Chromium (WebKit и Firefox не
ставятся и не гоняются).

**Дополнено 24.08.2026 при слиянии блока офсайта.** Два набора, написанных до
контракта, попали под него и подтвердили диагноз этого пункта: `offsite`
(127 проверок) и `deps` (12) заканчивались строкой «OK: N   FAIL: M», которую
раннер не читает, и получали приговор NO_REPORT — то есть выполненная работа
не засчитывалась вовсе. Оба переведены на канонический финал, `offsite` получил
преамбулу с кодом 77 и поимённой причиной, `deps` на отсутствии
`requirements.lock` даёт канонически посчитанный ПРОВАЛ, а не пропуск (D-42).
Сторожа стоят в `tests/test_runner.py` (48 → 70 проверок) и требуют
канонического финала от КАЖДОГО набора из `SUITES`, а от `.github/workflows/ci.yml`
— `--require-all` без единого `--allow-skip`.

**Фоновые падения, названные поимённо** (проверено 24.08.2026 прогоном чистого
клона `origin/main` `5706030` на той же машине и в том же окружении — числа
совпали до проверки):

* `planner` — 286 OK, **1 FAIL**, «накладные не меняют потребность в штуках».
  Одинаково на main, на ветке и в CI на ubuntu. Не расследовано;
* `backup` — на macOS 16 OK, 5 FAIL (WAL, ротация, разворачивание копии);
  на ubuntu в CI тот же набор даёт 21 OK, 0 FAIL. Это macOS-специфика, и она
  из того же корня, что остаточная часть выше: скрипты `deploy/` написаны под
  GNU-утилиты.

**`test_ui.py` ждёт фиксированными паузами и потому чувствителен к нагрузке.**
24.08.2026 в одном из трёх параллельных прогонов на macOS (`--jobs 3`, машина
занята) блок «подписи порогов — из настроек» дал 2 FAIL: после `page.reload()`
и паузы 2500 мс страница ещё не успела применить настройки, и проверка
прочитала **статическую разметку** `templates/stocks.html:209`
(`&gt; 5 000 ₽/день`), которую JS заменяет строкой 347. Продуктового дефекта
здесь нет — тот же набор даёт 24 OK, 0 FAIL и в одиночку, и в менее
загруженном прогоне, и в CI на ubuntu. Но набор теперь обязательный, а
фиксированные паузы в нём — вся стратегия ожидания, не только в этом блоке.
Пока это не переписано на ожидание условия, редкое ложное падение `ui`
возможно, и **лечить его исключением `--allow-skip ui` нельзя** (D-42): это
вернёт ровно ту дыру, ради которой всё делалось.

---

## D. Поддерживаемость

### MAINT-1. Девять рабочих страниц вне общего каркаса
**[подтверждено]** · влияние: высокое · стоимость: 2–3 недели

Развёрнуто в PRODUCT_STATUS §2. Десять копий экранирования, шесть обёрток над
`fetch`, восемь реализаций склонений, ~1050 строк продублированного CSS,
добавление пункта меню = десять файлов. Отсюда же три производных дефекта:
отсутствие обработки истечения сессии на рабочих страницах, отсутствие поддержки
встроенного режима МойСклада там же и четыре несовместимых порядка размеров.

**Важная оговорка про очерёдность:** пока деплой остаётся ручной загрузкой файлов
(OPS-3), этот рефакторинг делать опасно — он перемещает много файлов сразу.
Сначала пайплайн, потом каркас.

### MAINT-2. Крупные модули со смешением слоёв
**[подтверждено]** · влияние: среднее · стоимость: постепенно

`analytics.py` ~2000 строк, `ms_sync.py` ~1900, `api.py` ~1750, `order_planner.py`
~1120, `routes_extra.py` ~1140. Проблема не в размере, а в том, что доменная
логика живёт в роутерах, роутер импортирует приватные имена другого роутера,
ORM-модель объявлена в роутере, DDL — в шести модулях (ARCHITECTURE §3).

Правильный образец в проекте уже есть — чистая функция расчёта плана заказа,
не ходящая в БД. Её стоит считать целевым паттерном и распространять
**оппортунистически**, по мере работы над модулем, а не отдельной фазой.

### MAINT-3. Скрытое, мёртвое и битые ссылки на них
**[подтверждено]** · влияние: низкое, но задевает пользователя · стоимость: часы

Три скрытые/мёртвые страницы, два API без потребителей, механизм подмены ответов
API из режима превью остался в боевом коде. И живой дефект: «Мастер заказа» после
создания заказа ведёт на страницу, отдающую 404.

### MAINT-4. Заглушки шаблонов в пути поиска
**[подтверждено]** · влияние: низкое, коварное · стоимость: минуты

Каталог временных заглушек до сих пор стоит вторым путём поиска шаблонов, и все
десять его файлов дублируют имена живых. Удаление или переименование настоящего
шаблона приведёт не к ошибке, а к тихому показу заглушки.

### MAINT-5. Гигиена репозитория
**[подтверждено]** · влияние: низкое · стоимость: час

Около 9,5 МБ мусора в индексе (≈ 73 % рабочего дерева): 30+ скриншотов, девять
одноразовых скриптов проверки, **закоммиченный лог сервера**. Список игнорирования
написан под другой набор артефактов, чем тот, который в итоге закоммичен.

---

## E. Документация врёт

**[подтверждено]** · влияние: среднее для аудита и онбординга новых разработчиков

* `SPEC.md` лежит в корне, читается как действующий контракт («отклоняться нельзя»),
  но описывает состояние конца июля: другой набор таблиц, другую формулу
  потребности, другую библиотеку хеширования, страницу, которой больше нет.
* README обещает несуществующий файл деплоя, называет неверное число тестов
  и не упоминает пять тестовых файлов из семи; путается между двумя хостингами.
* Комментарии в трёх местах говорят «пять уроков» при фактических шести
  и «миграции убраны с импорта», когда одна осталась.
* Внутренний докстринг обещает порог затоварки 100 дней, константа — 90.
* Формат сообщения для авто-высоты во встроенном режиме помечен в коде как
  **угаданный** и не сверенный с документацией.

---

## Сводка приоритетов (моя оценка)

| Ранг | Пункт | Почему сюда |
|---|---|---|
| 1 | ~~DATA-3~~ | закрыт 23.08.2026 |
| 2 | OPS-3 | блокирует лечение всего остального |
| 3 | OPS-4 | открыт: код готов, но копия ни разу не уезжала с машины |
| 4 | ~~DATA-1, DATA-2~~ | закрыты 24.08.2026 (D-36, D-37); у DATA-1 остался живой contract-test `syncId` как условие слияния |
| 5 | ~~SEC-1~~ | закрыто: чужой production_id затирается, путь воспроизведён тестом |
| 6 | DATA-4, DATA-5 | доверие к цифрам — сама суть продукта |
| 7 | OPS-1, OPS-2 | дёшево, снимает целый класс инцидентов |
| 8 | DATA-11 | продукт спорит сам с собой на глазах у клиента |
| 9 | MAINT-1 | дорого, но только после OPS-3 |
| 10 | остальное | по мере касания |

## Чего в этом списке заведомо не хватает

* **Производительность.** Профилирования не было. Настораживают: пересчёт
  снапшота без защиты от параллельного пересчёта, три полных расчёта плана на
  каждое изменение анкеты, сложение агрегатов в Python вместо SQL. На каких
  объёмах это станет проблемой — не знаю.
* **Поведение на PostgreSQL.** И тесты, и прод — на SQLite; PostgreSQL заявлен
  в README, но не используется. Известен как минимум один дефект, из-за которого
  приложение на PostgreSQL вообще не стартовало (исправлен). Переезд на
  PostgreSQL сейчас — непроверенная операция, а на SQLite остаются свои риски:
  блокировки при записи, отсутствие проверки внешних ключей по умолчанию,
  единственный файл как единственная точка отказа.
* **Реальное поведение МойСклада.** Всё, что здесь про интеграцию, выведено из
  кода и из макета. Часовой пояс поля даты документа, наличие поля склада во всех
  типах документов, семантика частичных приёмок — не проверены на живом API.
