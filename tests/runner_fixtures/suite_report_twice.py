# -*- coding: utf-8 -*-
"""Отчёт в выводе упомянут дважды: сначала в тексте проверки, потом по делу.

Так пишет tests/test_runner.py: он печатает разбираемые строки в заголовке
проверки, и среди них есть «ИТОГО: 0 OK, 0 FAIL». Приговор обязан выноситься
по ПОСЛЕДНЕМУ отчёту — итог набор подводит в конце, а не в середине.
"""
print("  OK   classify('ИТОГО: 0 OK, 0 FAIL', rc=0) = FAIL")
print("  OK   разбор строки «ИТОГО: 1 OK, 9 FAIL» не путается")
print("\nИТОГО: 2 OK, 0 FAIL")
raise SystemExit(0)
