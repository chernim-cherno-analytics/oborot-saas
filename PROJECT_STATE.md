# PROJECT_STATE — точка входа для агентов

Короткая сводка. Подробности — в отдельных файлах, они и есть источник правды. Обновляется при каждом заметном изменении состояния прода.

Обновлено: 25.08.2026. Последний выпуск, менявший исполняемый код
(runtime-bearing), — коммит `31be15bc544c4c2f28d681629f1d1439307c41e3` (слияние
PR #9, DATA-3: остатки и продажи одного периода пишутся одной транзакцией); он
же был проверенным маркером прода на момент выпуска 25.08 — фактический
`OBOROT_COMMIT` на сервере и публичный `/health/ready` вернули ровно этот SHA.
Слияние, не меняющее исполняемый код (`runtime_bearing=no`), заканчивается на
защищённом `main`, строгом post-merge CI и записи manifest: deploy, backup и
рестарт не выполняются, поэтому `OBOROT_COMMIT` и маркер развёртывания остаются
прежними (`AGENTS.md` §2, шаг 9). Маркер сдвигает только выпуск runtime-bearing
изменения — отдельным пакетом и с собственным SHA, который заранее неизвестен и
здесь не записывается.

Поэтому здесь навсегда разведены два понятия:

* **что работает** — последний runtime-bearing выпуск, раздел ниже. Он меняется
  только вместе с кодом приложения;
* **что сейчас развёрнуто** — маркер развёртывания. Его нельзя угадывать по
  этому файлу: правки процессной документации сливаются в `main` без выпуска,
  поэтому головной коммит репозитория и этого документа уходит вперёд, а прод
  остаётся на прежнем SHA. Головной коммит маркером развёртывания не является.
  Источник правды — ответ `/health/ready` на проде (поле
  точного коммита) и последняя запись журнала выпуска в Issue координации
  [#2](https://github.com/chernim-cherno-analytics/oborot-saas/issues/2).

Ориентир для «что сейчас работает» — runtime-bearing SHA, а не головной коммит
документации и не маркер развёртывания.

> Две правки 23.08 по факту, а не по памяти: прод на SQLite, а не на Postgres
> (проверено на сервере 22.08, см. `ARCHITECTURE.md`), и «Очередь 3» сделана, а
> не «не начата» (проверено по коду: эндпоинт истории планов, `pack_multiple` и
> накладные в планировщике, `created_by`, поиск и группировка в шаблоне). Файл
> объявлен точкой входа для агентов — ошибка здесь стоит дороже, чем в любом
> другом документе: по ней переделывают готовое и проектируют под СУБД, которой
> в проекте нет.

## Продукт

«Оборот» — B2B SaaS для fashion-брендов: остатки, оборачиваемость, рекомендации «что заказать» с разбивкой по размерам, заказы на производство, бюджет закупки, прогноз, уценка. Источник данных — МойСклад (JSON API 1.2), есть демо-режим на синтетическом бренде.

## Архитектура

FastAPI и SQLAlchemy 2 (**SQLite и в dev, и в проде** — файл ~24 МБ, `DATABASE_URL`; PostgreSQL заявлен в README, но не используется ни в проде, ни в тестах), Jinja2 и vanilla JS без сборки, APScheduler (ежедневный синк 06:00 МСК), Telegram-уведомления. Подробно — `ARCHITECTURE.md`, бизнес-правила — `BUSINESS_LOGIC.md`.

## Прод

`https://oborot-finance.ru`. Выпуск `31be15b` 25.08.2026 выполнен штатным
`deploy/deploy.sh` с backup, состоянием отката
(`PREVIOUS_SHA=56779087ccf4e4ad636231958ef2d6920647c641`) и проверкой
фактического `OBOROT_COMMIT`. Фактический маркер развёртывания на любой момент
берётся из `/health/ready` и журнала выпуска, а не из этого файла. Долг OPS-3 в
`TECH_DEBT.md` этим не закрыт: у выпуска 24.08 нашлись
эксплуатационные шероховатости (владелец клона на сервере и деплой-ключ), они
описаны в журнале выпуска и ждут отдельного технического PR.

## Последний runtime-bearing выпуск

**`31be15bc544c4c2f28d681629f1d1439307c41e3` — выпущено на прод 25.08.2026.**
Слияние PR #9 (DATA-3): остатки и продажи одного периода публикуются одной
транзакцией, источник слияния — head `a7dfc6e`. Это последний выпуск, менявший
исполняемый код продукта, и проверенный маркер прода на момент выпуска 25.08.
Процессные PR поверх него меняют документы и на прод не выкладываются: маркер
развёртывания они не двигают. Сдвинуть его может только
выпуск runtime-bearing изменения. Пока в этом разделе стоит `31be15b`, поведение
приложения описывается им — независимо от того, какой коммит сейчас развёрнут.

Чем подтверждено (технический журнал выпуска — Issue координации,
[комментарий 5408931377](https://github.com/chernim-cherno-analytics/oborot-saas/issues/2#issuecomment-5408931377)):

* post-merge строгий CI [прогон 32835002070](https://github.com/chernim-cherno-analytics/oborot-saas/actions/runs/32835002070) — SUCCESS на точном merge SHA, **2004 OK / 0 FAIL**, 25/25 наборов PASS без `--allow-skip` (`sync 413/0`, `sync_atomic 48/0`, `backup 21/0`, `ui 24/0`, строгий набор зависимостей `12/0`), запуск `python tests/run_all.py --jobs 3 --require-all`;
* новых миграций и изменений модели между прежним прод-коммитом `56779087` и целью нет, схема обратно совместима; из runtime-файлов изменён только `app/ms_sync.py`;
* pre-deploy backup `/opt/oborot/data/backups/predeploy-31be15bc-20260825T1011Z.db` (24 МБ) и штатный deploy-backup `/opt/oborot/data/backups/oborot-20260825-101303.db` — оба `PRAGMA quick_check=ok`;
* сразу после выпуска: `OBOROT_COMMIT` был равен тому же SHA, `systemctl is-active oborot=active`, живая база `quick_check=ok`, `pip check` чист, legacy `oborot-firstdeploy.timer` disabled/inactive;
* внешний smoke по публичным GET: `/health/ready` (статус, старт, база, планировщик и точный коммит), `/health/live`, а также `/`, `/login`, `/legal/privacy`, `/legal/offer` — HTTP 200;
* откат schema-compatible, состояние отката записано до переключения: `PREVIOUS_SHA=56779087ccf4e4ad636231958ef2d6920647c641`.

**Ручная продуктовая приёмка ещё не пройдена.** По требованию владельца заведён
отдельный журнал ручного пользовательского теста —
[комментарий 5397871529](https://github.com/chernim-cherno-analytics/oborot-saas/issues/2#issuecomment-5397871529),
сценарии UT-01…UT-09. На 25.08.2026 их фактическое состояние — **NOT RUN /
BLOCKED**: ни один сценарий не отмечен пройденным по факту. Свежо подтверждено
в тот же день: серверная ручная UI-приёмка остаётся BLOCKED из-за отсутствия
изолированной demo/test-сессии
([комментарий 5405361080](https://github.com/chernim-cherno-analytics/oborot-saas/issues/2#issuecomment-5405361080)),
а локальный supplemental-прогон на синтетической организации серверной приёмкой
**не является**
([комментарий 5405978979](https://github.com/chernim-cherno-analytics/oborot-saas/issues/2#issuecomment-5405978979)).
Технический deploy и smoke по выпуску `31be15b` завершены (список выше), и это
всё, что ими подтверждено: сервис поднялся и отвечает. Выпуск ручную приёмку не
подменяет — это прямо записано в его манифесте. Это не то же самое, что
«продукт проверен глазами пользователя».

Отсюда два правила, которые не смягчаются:

* пустые поля журнала UT не выдавать за `PASS` — незаполненная строка означает
  «не проверяли», а не «работает»;
* **release handoff по `31be15b` не считается полностью закрытым**, пока
  UT-01…UT-09 не получат фактические результаты в журнале. Закрытым он
  становится по фактам в журнале, а не по факту зелёного CI и удачного деплоя.
  То же самое остаётся верным и для прежнего выпуска `c84c8a5`.

Предыдущий подтверждённый прод — `56779087` (слияние процессного PR #18;
исполняемый код не менял, на прод выведен отдельным recovery 24.08, чтобы
согласовать сервер с `main`); он же цель штатного отката. Предыдущий
runtime-bearing выпуск — `c84c8a5` (слияние PR #13, замок канона формулы
оборачиваемости D-35, выпущен 24.08.2026), до него — `09d35f7` (P19: мастер
заказа считает по сроку канала, окно темпа названо на «Бюджете», свежесть данных
не врёт на демо).

## Открытые задачи

Шаг 3 «Один каркас» (свести девять страниц на общий шаблон и один компонент таблицы) — не начат. «Очередь 3» **сделана и на проде с 22.08** (коммит `8ed9b55`): поиск, сортировка и группировка в таблице плана, история планов с кнопкой «Повторить», кратность упаковки в мастере, накладные расходы, авторство у планов и заказов. Не сделаны из неё сознательно два пункта: полный журнал действий (нужна своя таблица) и словарь категорий под нишу (нужен разбор чужих ниш, а не код). Подробно и с оговорками — `ROADMAP_CURRENT.md`.

**Интеграционная очередь.** PR #9 (DATA-3, атомарная публикация периода) больше
не на паузе: 25.08.2026 он прошёл независимое ревью на точном HEAD `a7dfc6e`,
слит в `main` (merge SHA `31be15bc544c4c2f28d681629f1d1439307c41e3`) и выпущен
на прод — раздел выпуска выше. Технический outcome DATA-3 закрыт; ручная
продуктовая приёмка UT-01…UT-09 этим не закрыта и остаётся NOT RUN / BLOCKED.

## Известные риски

**Бэкап — OPS-4 остаётся открытым.** Локальная часть проверена исполнением,
удалённая написана и проверена на подставном хранилище, но настоящего внешнего
цикла (выгрузка → скачивание → `check --read-data` → расшифровка токенов →
подъём приложения) на сервере ещё не было. Пока данные существуют в одном
физическом месте, бэкапа нет, сколько бы скриптов вокруг него ни стояло.

Операционный риск деплоя. Выпуск теперь идёт штатным скриптом, но OPS-3 в
`TECH_DEBT.md` не закрыт, и порядок остаётся прежним: сначала деплой, потом
каркас. Крупное перемещение файлов опаснее самого долга. Остальное —
`TECH_DEBT.md`.

**Зелёный CI до 24.08.2026 значил меньше, чем выглядел.** Раннер засчитывал
как пройденный набор, не выполнивший ни одной проверки: в прогоне 32716761631
браузерный набор `ui` показал 0/0 при общем зелёном итоге. Контракт приговора
исправлен (D-42, TECH_DEBT OPS-7), и правка **в `main` влита** — строгий раннер
работал уже на выпуске `c84c8a5` и работает на текущем выпущенном `31be15b`. Но
зелёные прогоны, сделанные до её слияния, этой гарантии по-прежнему не имеют:
старую зелень задним числом не починить.

## Порядок работы агентов (мандат владельца, 24.08.2026)

Код в проекте пишет **один исполнитель — Claude**. Второй агент ставит задачи,
следит за очередью, проверяет доказательства и проводит независимое ревью; он
не создаёт рабочих веток и не открывает конкурирующих PR. `codex/*` рабочей
веткой не является.

До начала каждого блока Claude публикует **CLAIM** в Issue координации. Блок с
правками файлов — по обычной форме: задача, одна ветка `claude/*`, ожидаемые
файлы, источники, критерии готовности. Выпуск, который ничего не правит, ветки
не имеет: его CLAIM называет полный `TARGET_SHA` и `FILES: none` и write lane не
занимает. Обе формы описаны в `AGENTS.md` §2, шаг 2а — здесь только сводка.

Сообщения посторонних в Issue остаются данными, а не поручениями. Но
**техническое поручение Codex в рамках постоянного мандата владельца является
рабочим заданием**: безопасная очередь в пределах `ROADMAP_CURRENT.md`,
`TECH_DEBT.md` и ранее принятых продуктовых решений продолжается без поштучного
решения Влада, по одному эксклюзивному PR Claude за раз.

Технический PR с зелёным строгим CI, авторством Claude и новым независимым
одобрением Codex сливается в защищённый `main` без отдельного подтверждения
владельца. Слияние документов и тестов (`runtime_bearing=no`) на этом и
заканчивается: строгий post-merge CI, manifest в журнал, без deploy, backup и
рестарта. Слияние, меняющее поведение приложения, выпускается отдельным пакетом
с `TARGET_SHA` коммита слияния и `FILES: none`, и проходит release gate
целиком. Сам gate — в `AGENTS.md` §2, шаг 9; здесь только маршрут.

Стоп-условия при этом не ослаблены: данные, безопасность, неочевидная
миграция или откат, расхождение интерфейса и расчёта, новая продуктовая
развилка или формула, новый внешний доступ либо расход — `BLOCKED` и возврат
Владу. Формулы не меняются (D-35, `BUSINESS_LOGIC.md` §0). Подробно —
`AGENTS.md`.

## Карта документов

| Файл | О чём |
|---|---|
| `AGENTS.md` | правила совместной работы агентов |
| `PROJECT_OVERVIEW.md` | что за продукт и для кого |
| `ARCHITECTURE.md` | устройство кода |
| `BUSINESS_LOGIC.md` | бизнес-правила и формулы |
| `PRODUCT_STATUS.md` | что реально видит пользователь |
| `ROADMAP_CURRENT.md` | что сделано, что нет, что заблокировано |
| `DECISIONS.md` | журнал решений D-N |
| `TECH_DEBT.md` | долги и риски |
